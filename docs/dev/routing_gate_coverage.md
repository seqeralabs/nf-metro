---
title: "Routing gate coverage matrix"
description: Coverage matrix tracking which routing families have invariant gate-arm tests and their current pass/fail status.
sidebar:
  label: "Routing gate coverage"
  order: 5
---

# Routing gate coverage matrix

Auto-generated by `scripts/routing_gate_coverage.py` under CPython 3.11 with `PYTHONHASHSEED=0` (the arc model is interpreter-specific and operand-level coverage is hash-seed sensitive). Do not edit by hand; run the script to regenerate.

Each row is a branch point (a *gate*) in a `layout/routing/` dispatch handler or post-pass. A gate has two or more arms; the cells list how many corpus fixtures exercise each arm. An arm reached by **0 fixtures** is an un-exercised gate arm: either no shipped topology takes that path (author a fixture, or confirm it is defensive/dead).

**1813/2614** gates fully exercised (both/all arms hit by some fixture); **801** gates have at least one un-exercised arm.

Modules scoped to routing decision gates; `invariants.py` (the `validate=True` checker) and `__init__.py` are excluded.

The Triage column carries a curated verdict for gaps no fixture can close: **defensive** (a guard arm a valid topology never violates), **candidate-dead** (no constructible topology reaches it; left in place pending a separate deletion review), or **needs-review** (not yet classified). A blank cell means the gap is still open for a fixture. **273** gaps carry a triage verdict.

## `arranger.py`

1/1 gates fully exercised.

All gates have every arm exercised by the corpus.

## `bundle.py`

5/7 gates fully exercised.

Gates with an un-exercised arm:

| Line | Gate | Un-exercised arm(s) | Triage |
| ---: | --- | --- | --- |
| 51 | `if abs(dx) > COORD_TOLERANCE and abs(dy) > COORD_TOLERANCE:` | `->L52` |  |
| 281 | `if len(centerline) < 2:` | `->L282` | **defensive** -- Single enforcement point for the bundle-centreline contract: a fanned bundle needs at least one leg, i.e. a >=2-vertex centreline. All three public builders (build_concentric_bundle / build_tapered_bundle / build_offset_bundle) funnel through _fan_bundle, which owns this one guard in place of the four per-builder re-checks the corpus never took (#1438). Every caller builds a >=2-vertex polyline by construction, so the raise arm stays un-exercised. |

## `centrelines.py`

7/10 gates fully exercised.

Gates with an un-exercised arm:

| Line | Gate | Un-exercised arm(s) | Triage |
| ---: | --- | --- | --- |
| 73 | `if (` | `->L81` |  |
| 82 | `if place_at_source:` | `->L86` |  |
| 432 | `if route.curve_radii is not None:` | `->L434` |  |

## `common.py`

152/196 gates fully exercised.

Gates with an un-exercised arm:

| Line | Gate | Un-exercised arm(s) | Triage |
| ---: | --- | --- | --- |
| 154 | `if junction is None:` | `->L155` |  |
| 157 | `if len(pts) < 2:` | `->L158` |  |
| 177 | `if not any(` | `->L185` |  |
| 213 | `for edge in graph.edges_from(port_id):` | `->L217` |  |
| 215 | `if not consumer.is_port:` | `->L213` |  |
| 399 | `if top is None or bottom is None:` | `->L400` | **defensive** -- Defensive in iter_inter_row_gaps (consumed by inter_row_gap_upper_row and _inter_row_gap_band): adjacent rows in the sorted section-row set both contain sections, so row_bottom_edge / row_top_edge resolve; the None skip arm guards a row with no sections that the iteration never yields. |
| 566 | `if s.bbox_h <= 0:` | `->L567` | **defensive** -- Degenerate-bbox guard in _grid_row_bands: a laid-out section always has bbox_h > 0, so the skip arm only protects against a zero/negative-height section that the layout never produces. |
| 685 | `if n == 0:` | `->L686` | **defensive** -- Empty-bundle guard: all callers (normalize.py 291/1648, inter_section_handlers.py 783/796/2132) derive bundle_widths from a non-empty bundle line count, so n is always >=1; the empty-list arm is never taken. |
| 687 | `if bundle_index < 0 or bundle_index >= n:` | `->L688` | **defensive** -- Contract guard raising IndexError on an out-of-range bundle_index. Callers always pass an in-range index (0<=i<n); the raise arm fires only on a caller-contract violation no valid call produces. |
| 975 | `if abs(pts[k - 1][0] - x0) > COORD_TOLERANCE:` | `->L976` | **defensive** -- Interior horizontal segments only occur in 6-point bypass U-shapes whose flanking legs are always vertical (same x on each side); routes with non-vertical flanks are normalize_exempt and filtered out. The prev-flank-non-vertical reject in iter_horizontal_trunks is unreachable. Reclassified candidate-dead -> defensive (#762): corpus instrumentation across 128 fixtures confirms the un-exercised arm is never taken. |
| 977 | `if abs(pts[k + 2][0] - x1) > COORD_TOLERANCE:` | `->L978` | **defensive** -- Mirror of the prev-flank guard: a bypass trunk's trailing flank is always vertical, so the next-flank-non-vertical reject in iter_horizontal_trunks is unreachable. Reclassified candidate-dead -> defensive (#762): corpus instrumentation across 128 fixtures confirms the un-exercised arm is never taken. |
| 1022 | `if abs(y4 - y3) > COORD_TOLERANCE or abs(x4 - x3) <= COORD_TOLERANCE:` | `->L1023` | **defensive** -- Trunk-not-horizontal guard in port_peeloff_tail, reached only after the earlier port-lead and riser checks returned early. In an orthogonal route segments alternate H/V, so the segment preceding a vertical riser is always horizontal; instrumenting the corpus the guard is reached but the non-horizontal-trunk arm never fires. Defensive against a degenerate collinear tail the router does not produce. |
| 1114 | `if len({round(value, 6) for value in port_ys}) != n:` | `->L1115` | **defensive** -- A complete entry port assigns every distinct line a unique station-offset slot. Duplicate port-Y values therefore require a malformed route set that bypassed station offset allocation; test_invalid_opposing_candidate_is_rejected[duplicate-port-slot] exercises the rejection directly. |
| 1116 | `if port_ys[-1] - port_ys[0] > (n - 1) * step + COORD_TOLERANCE:` | `->L1117` | **defensive** -- Station offset allocation gives a complete n-line entry port one contiguous n-slot band, so its port-Y extent is exactly bounded by (n-1)*step. The wider-band reject is exercised by test_invalid_opposing_candidate_is_rejected[noncontiguous-port-band]. |
| 1119 | `if max(peel_xs) - min(peel_xs) > (n - 1) * step + COORD_TOLERANCE:` | `->L1120` | **defensive** -- A complete destination bundle receives one contiguous n-slot channel band from gap materialization, so its peel-X extent cannot exceed (n-1)*step. The wider-band arm protects hand-built or corrupted route geometry and is exercised by test_invalid_opposing_candidate_is_rejected[noncontiguous-channel-band]. |
| 1123 | `if shared_hi - shared_lo < min_common_approach - COORD_TOLERANCE:` | `->L1124` | **defensive** -- Inter-row placement reserves at least the two-corner runway used by side-port peel-off handlers, so complete opposing entry tails share at least min_common_approach vertically. A shorter overlap is malformed settled geometry; test_invalid_opposing_candidate_is_rejected[insufficient-common-approach] exercises the guard. |
| 1240 | `if trunk_ys[-1] - trunk_ys[0] <= COORD_TOLERANCE:` | `->L1241` | **needs-review** -- Degenerate single-Y trunk band in iter_port_peeloff_bundles: a peel-off bundle is yielded only when its members ride trunks at distinct depths, so the equal-depth arm guards a same-Y bundle the trunk-slot materialization never leaves; render-diff confirms no visual change. |
| 1362 | `if pinned_bases and any(` | `->L1365` |  |
| 1375 | `if k == 0 or k + 2 >= len(points):` | `->L1376` |  |
| 1559 | `elif tgt_sec:` | `->L1567` | **defensive** -- The else (col_key=round(sx)) needs an L-shaped (dx!=0,dy!=0) inter edge whose target resolves to no section. Only exit_port->junction edges lack a target section, and junction positioning makes those axis-aligned (same-X RIGHT exit or same-Y BOTTOM exit), never L-shaped, so the else is unreachable. Reclassified candidate-dead -> defensive (#762): corpus instrumentation across 128 fixtures confirms the un-exercised arm is never taken. |
| 1659 | `if dx > 0:` | `->L1662` | **defensive** -- Leftward (dx<=0) near-source fallback: the symmetric mirror of the rightward fallback. Reached only when section info is unavailable AND no adjacent column resolves; valid layouts take the rightward fallback, but the leftward mirror is kept for robustness. |
| 1670 | `if st is not None and st.is_port:` | `->L1668` | **defensive** -- Null/non-port guard: after _resolve_sections both endpoints of an inter-section edge are ports/junctions and graph.stations.get() resolves them, so the False (None or non-port) arm only guards a malformed pre-resolve graph. |
| 1719 | `if sec.bbox_w <= 0:` | `->L1720` | **defensive** -- Zero-width-section guard: the loop scans all graph.sections, but after compute_layout every placed section has bbox_w>0, so the skip arm only guards an empty/unplaced section that valid layouts don't produce. |
| 1767 | `if sec.bbox_w <= 0:` | `->L1768` | **defensive** -- Zero-width-section guard in the squeeze re-centring loop: like its #1 twin, after compute_layout every placed section has bbox_w>0, so the skip arm only guards an empty/unplaced section that valid layouts do not produce. |
| 1801 | `if not src.is_port:` | `->L1799` |  |
| 1817 | `for i in range(len(pts) - 1):` | `->L1818`, `->L1834` | **defensive** -- point_on_polyline is used only by the --animate renderer (render/animate.py); the matrix's render-path coverage sweep calls render_svg without animation, so none of its branches are reached. Exercised instead by test_animation.py / test_routing.py. |
| 1822 | `if seg_len2 == 0:` | `->L1823`, `->L1826` | **defensive** -- point_on_polyline degenerate-segment branch, reached only via the --animate renderer (render/animate.py); the render-path coverage sweep doesn't animate. Exercised by test_animation.py / test_routing.py, not the corpus. |
| 1823 | `if abs(point[0] - ax) <= tol and abs(point[1] - ay) <= tol:` | `->L1824`, `->L1825` | **defensive** -- point_on_polyline zero-length-segment hit test, reached only via the --animate renderer; the render-path coverage sweep doesn't animate. Exercised by test_animation.py / test_routing.py. |
| 1827 | `if t < -0.01 or t > 1.01:` | `->L1828`, `->L1829` | **defensive** -- point_on_polyline parameter-range branch, reached only via the --animate renderer; the render-path coverage sweep doesn't animate. Exercised by test_animation.py / test_routing.py. |
| 1832 | `if abs(point[0] - proj_x) <= tol and abs(point[1] - proj_y) <= tol:` | `->L1817`, `->L1833` | **defensive** -- point_on_polyline projection hit test, reached only via the --animate renderer; the render-path coverage sweep doesn't animate. Exercised by test_animation.py / test_routing.py. |
| 1910 | `if all_in_range:` | `->L1914` | **defensive** -- cross_row bypass guard: the False arm (no sections in the column range -> return clearance) needs an empty [lo,hi] column span, which a valid multi-section pipeline routing cross_row never has; a degenerate-range guard. |
| 1937 | `if endpoints:` | `->L1940` | **defensive** -- Non-cross_row bypass guard: the endpoints list always includes the source and target sections (both in src_row), so it is never empty; the default-clearance arm only guards a degenerate empty endpoint set. |
| 1950 | `if src_row is not None:` | `->L1962` | **defensive** -- Header-clamp guard: the False arm (src_row is None) needs an unresolvable source section, but inter-section bypass sources always resolve to a grid row after layout; a guard against an unresolved source that doesn't occur. |
| 1957 | `if safe_cap >= row_bottom:` | `->L1960` | **defensive** -- The false arm is a defensive midpoint fallback for an inter-row gap too narrow to clear both an upper-row bypass and the lower section header. Placement reserves the routing band before bypass geometry is computed, so settled corpus layouts provide enough clearance for the safe-cap arm. |
| 2056 | `if section.bbox_w <= 0:` | `->L2057` | **defensive** -- Degenerate-bbox guard in _h_segment_penetrates_section (the single-section predicate shared by _h_segment_crosses_other_section and _packed_cell_mate_obstructs): sections always have positive width after layout. |
| 2082 | `if station is None:` | `->L2083` | **defensive** -- None-station guard: every production caller passes a concrete Station (grep-verified); the None arm only guards the typed Station\|None parameter. |
| 2088 | `for port in junction_source_ports(graph, station.id):` | `->L2107` | **defensive** -- Upstream loop-exit in resolve_section: every fan-out junction is emitted with an exit_port->junction edge whose source port carries a section id, so junction_source_ports always yields a sectioned port and the loop returns; the fall-through to the type-contract return None is the never-taken no-upstream fallback. |
| 2090 | `if sec:` | `->L2088` | **defensive** -- Upstream section-lookup guard: graph.sections.get(section_id) returning None requires a section_id absent from graph.sections, a corrupted state no valid parse produces; null-guard. |
| 2096 | `for e in graph.edges:` | `->L2107` | **defensive** -- No-preference graph.edges scan loop-exit: a fan-out junction always has a sectioned incident port (its exit/entry ports carry a section_id), so the loop always returns and never exhausts graph.edges; the fall-through to the type-contract return None is the never-taken no-neighbour fallback. |
| 2097 | `if e.source == station.id:` | `->L2098` | **defensive** -- Junction-as-source arm of the no-preference scan: fan-out junctions are emitted exit_port->junction before junction->entry_port, so graph.edges encounters the junction as a target (elif arm) first and returns; the source arm is a cheap structural guard whose deadness rests on emit order, not a hard type invariant. |
| 2103 | `if other.section_id:` | `->L2096` | **defensive** -- The prefer_upstream=False scan reads a junction's incident neighbour on either side, always a sectioned station or port; the section-less (False) arm only guards a junction-to-junction edge valid construction never produces. |
| 2105 | `if sec:` | `->L2096` | **defensive** -- prefer_upstream=False section-lookup guard: graph.sections.get(section_id) returning None requires a missing section_id (corrupted state); null-guard. |
| 2115 | `if section is None:` | `->L2116` | **defensive** -- resolve_section_colrow accepts a station whose owning section may not resolve. Production routing passes resolved ports or junctions with an incident sectioned port, so the None return only protects a malformed or incomplete graph. |
| 2214 | `if dy > 0:` | `->L2219` | **defensive** -- Multi-row upward-crossing arm: grid rows are assigned topologically and upward inter-section routes take other handlers (a same-row/around-section path), never reaching inter_row_channel_y's multi-row branch with dy<=0; verified by an upward multi-row fixture that bypasses this code. |

## `context.py`

81/109 gates fully exercised.

Gates with an un-exercised arm:

| Line | Gate | Un-exercised arm(s) | Triage |
| ---: | --- | --- | --- |
| 290 | `if not mst:` | `->L291` | **defensive** -- mjid ranges over junctions, a subset of junction_ids all present in graph.stations; the missing-station continue never fires. |
| 293 | `if tgt_col is None:` | `->L294` | **defensive** -- A merge junction always resolves to a grid column (it sits adjacent to its entry-port section); the unresolved-column continue never fires. |
| 351 | `if m_col is None:` | `->L352` | **defensive** -- Reached only for junctions in trunk_source, which by construction resolved a column, so m_col is never None and the skip arm never fires. |
| 363 | `if pred_col is None:` | `->L364` | **defensive** -- A merge feeder is a junction fed by an exit port, which always resolves a column, so the skip arm never fires. |
| 382 | `if ep and ep.is_entry:` | `->L380` | **defensive** -- Every outgoing edge of a classified convergence junction targets its sole entry port, so the non-entry arm never fires in the skip-edge pass. |
| 550 | `if port_st is None:` | `->L551` | **defensive** -- Ports listed in a section's entry_ports/exit_ports always have a backing station; the missing-station continue never fires. |
| 570 | `if not ctx.station_offsets:` | `->L571` | **defensive** -- Pairs with _get_offset's guard: compute_station_offsets is never empty on the render path, so _max_offset_at's early-return arm guards only the route_edges(station_offsets=None) call path used by tests. |
| 619 | `if port.section_id is None:` | `->L620`, `->L621` |  |
| 651 | `for pid in section.entry_ports:` | `->L652`, `->L654` |  |
| 652 | `if line_id in graph.station_lines(pid):` | `->L651`, `->L653` |  |
| 680 | `if port is not None:` | `->L681`, `->L684` |  |
| 685 | `for sid in section.station_ids:` | `->L686`, `->L690` |  |
| 687 | `if not station.is_port:` | `->L685`, `->L688` |  |
| 709 | `if section is None:` | `->L710` |  |
| 718 | `if sec and sec.grid_col >= 0:` | `->L720` | **needs-review** -- grid-col sentinel: a rail-mode section carries grid_col == -1 (it is placed outside the section grid), so the negative-column arm returning None is load-bearing, not dead. No corpus routing path resolves a rail section's column, so the arm is un-exercised, but a rail topology can reach it; do not fold into a non-optional accessor. |
| 726 | `if sec and sec.grid_row >= 0:` | `->L728` | **needs-review** -- grid-row sentinel: a rail-mode section carries grid_row == -1 (placed outside the section grid), so the negative-row arm returning None is load-bearing, not dead. No corpus routing path resolves a rail section's row, so the arm is un-exercised, but a rail topology can reach it; do not fold into a non-optional accessor. |
| 816 | `if sec is None:` | `->L817` | **defensive** -- _resolve_section_colrow is called for ports/junctions that always resolve to a section; the None-section early-return never fires (mirrors the grid-sentinel guards at L395/L403). |
| 862 | `if src.col is None or tgt.col is None:` | `->L863` | **defensive** -- _hop_needs_bypass is called only for endpoints whose columns the caller already resolved, so the no-column early-return never fires. |
| 957 | `if pst is None:` | `->L958` | **needs-review** -- pst = graph.stations.get(port.id): a port is always registered as a backing station, so the guard is never taken across the corpus. Kept as a station-id string read (no Edge in hand), the idiom deferred by the #1438 single-endpoint migration for a future station-id accessor; the sibling section lookup here was migrated to graph.section_for_port. |
| 1117 | `if not jst:` | `->L1118` | **defensive** -- jid ranges over junction_ids, all present in graph.stations; the missing-station continue never fires. |
| 1120 | `if src_col is None:` | `->L1121` | **defensive** -- A fan-out junction always resolves to a grid column; the unresolved-column continue never fires. |
| 1158 | `if not (tgt.is_port or edge.target in junction_ids):` | `->L1159` |  |
| 1161 | `if tgt_col is None:` | `->L1162` | **defensive** -- A junction's port/junction target always resolves to a grid column; the unresolved-column continue never fires. |
| 1263 | `if edge.line_id in line_pos:` | `->L1262` | **defensive** -- line_pos is built from the line IDs of the very all_outgoing list this loop iterates, so the membership test is always true: the reject/loop-back arm (L684) is an unreachable tautology and the enter-body static arc (L686) is a phantom of the real 685->687. Vestigial tautological guard. The deliberate gap is pinned by test_genuine_dead_arm_not_masked_as_covered (arc normalization must not merge the never-taken branch onto the live one). Reclassified defensive (#762). |
| 1292 | `if not (tgt.is_port or edge.target in graph.junction_ids):` | `->L1293` |  |
| 1336 | `if jst is None:` | `->L1337` |  |
| 1339 | `if src_row is None or col is None:` | `->L1340` | **defensive** -- In _compute_fan_corridors, a fanning junction always resolves to a section column and row; the unresolved continue guards a malformed graph the render path never produces. |
| 1383 | `if tgt_col is None:` | `->L1384` | **needs-review** -- Defensive: _fan_bypass_band skips a fan edge whose target has no resolvable grid column; every corpus fan target resolves a column, so the skip arm is unexercised. |

## `convergences.py`

171/315 gates fully exercised.

Gates with an un-exercised arm:

| Line | Gate | Un-exercised arm(s) | Triage |
| ---: | --- | --- | --- |
| 194 | `if edge_rank is None:` | `->L195` |  |
| 274 | `if owned:` | `->L268` |  |
| 290 | `if route is None:` | `->L291` |  |
| 311 | `if route is None:` | `->L312` |  |
| 325 | `if family is None:` | `->L326` |  |
| 334 | `if not runs:` | `->L335` |  |
| 356 | `if axis.axis is not DemandAxis.X:` | `->L357` |  |
| 373 | `if route.exit_lane_transition_plan_id is not None:` | `->L374` |  |
| 406 | `if not candidates:` | `->L407` |  |
| 422 | `if horizontal and abs(endpoint[1] - target[1]) <= COORD_TOLERANCE:` | `->L425` |  |
| 425 | `if vertical and abs(endpoint[0] - target[0]) <= COORD_TOLERANCE:` | `->L426`, `->L428` |  |
| 429 | `if elbow != endpoint and elbow != target:` | `->L430`, `->L431` |  |
| 435 | `if route.offset_regime is OffsetRegime.DEFERRED:` | `->L436` |  |
| 443 | `for rank, (start, end) in enumerate(zip(route.points, route.points[1:])):` | `->L459` |  |
| 446 | `runway <= COORD_TOLERANCE` | `->L449` |  |
| 454 | `if abs(prior[0] - start[0]) + abs(prior[1] - start[1]) > COORD_TOLERANCE:` | `->L458` |  |
| 456 | `if direction_axis(incoming) is not direction_axis(approach):` | `->L458` |  |
| 508 | `if exit_turn_geometry != _exit_turn_geometry(route):` | `->L509` |  |
| 520 | `if approach is None:` | `->L521` |  |
| 572 | `if direction in {Direction.R, Direction.L}:` | `->L576` |  |
| 601 | `if len(route.points) < 2:` | `->L602` |  |
| 611 | `if axis is DemandAxis.X:` | `->L617` |  |
| 612 | `if abs(start[1] - end[1]) > COORD_TOLERANCE:` | `->L613` |  |
| 617 | `if abs(start[0] - end[0]) > COORD_TOLERANCE:` | `->L618`, `->L619` |  |
| 621 | `if extent_end - extent_start > COORD_TOLERANCE:` | `->L600` |  |
| 632 | `if not segments:` | `->L633` |  |
| 646 | `if axis is DemandAxis.X:` | `->L656` |  |
| 713 | `if any(edge not in edge_order for edge in edges):` | `->L714` |  |
| 749 | `if not incoming_edges or not outgoing_edges:` | `->L750` |  |
| 755 | `if group.line_id not in line_order:` | `->L756` |  |
| 772 | `if trunk_edge_key is None:` | `->L773` |  |
| 821 | `if route is not None:` | `->L825` |  |
| 825 | `if route is None:` | `->L826` |  |
| 842 | `if any(` | `->L846` |  |
| 860 | `if (` | `->L865`, `->L877` |  |
| 865 | `for edge_key in incoming_edges:` | `->L866`, `->L869` |  |
| 1012 | `if (` | `->L1024` |  |
| 1053 | `if primary_reason is ConvergenceTrunkReason.OUTGOING_CONTINUATION:` | `->L1054` |  |
| 1055 | `if continuation.covered_by_member_id is not None:` | `->L1056`, `->L1078` |  |
| 1164 | `if topology is None:` | `->L1165` |  |
| 1231 | `if all(` | `->L1234` |  |
| 1261 | `or abs(start[1] - end[1]) > COORD_TOLERANCE` | `->L1268` |  |
| 1264 | `if direction_axis(incoming) is not landing.approach_axis:` | `->L1268` |  |
| 1373 | `if opening_follows_flank:` | `->L1374` |  |
| 1435 | `if landing.opening_turn_segment is None:` | `->L1436` |  |
| 1438 | `if landing.approach_axis is DemandAxis.X:` | `->L1442` |  |
| 1440 | `if runway < curve_radius - COORD_TOLERANCE:` | `->L1441` |  |
| 1462 | `for rank, landing in enumerate(landings):` | `->L1463`, `->L1470` |  |
| 1463 | `if landing.member_id not in member_ids:` | `->L1464`, `->L1465` |  |
| 1466 | `if reseated is None:` | `->L1467`, `->L1468` |  |
| 1498 | `if not on_central(landing.join_point):` | `->L1499` |  |
| 1503 | `if opening_segment is not None:` | `->L1512` |  |
| 1624 | `if axis is None:` | `->L1625` |  |
| 1634 | `if source is None:` | `->L1635` |  |
| 1666 | `if axis is None or axis.axis is not DemandAxis.X:` | `->L1667` |  |
| 1676 | `if primary is None:` | `->L1677` |  |
| 1689 | `or landing.opening_turn_segment is None` | `->L1691` |  |
| 1702 | `if reseated is not None:` | `->L1685` |  |
| 1744 | `obstacle.edge.line_id != landing.edge.line_id` | `->L1749` |  |
| 1745 | `or landing_segment is None` | `->L1749` |  |
| 1746 | `or obstacle_segment is None` | `->L1749` |  |
| 1747 | `or _direction(*obstacle_segment) is _direction(*landing_segment)` | `->L1749` |  |
| 1760 | `if reseated is None:` | `->L1761` |  |
| 1763 | `if landing.member_id == plan.primary_trunk_member_id:` | `->L1783` |  |
| 1803 | `if axis is None:` | `->L1804` |  |
| 1827 | `if coordinate is not None:` | `->L1832` |  |
| 1843 | `if plan.trunk_axis is None:` | `->L1844` |  |
| 1876 | `if coordinate is not None:` | `->L1845` |  |
| 1902 | `if flank_rank != 1:` | `->L1904` |  |
| 1913 | `if fork is None:` | `->L1914`, `->L1922` |  |
| 1990 | `if forked:` | `->L1991` |  |
| 1991 | `if abs(flank_coordinate - landing_coordinate) <= COORD_TOLERANCE:` | `->L1992`, `->L1993` |  |
| 1995 | `if abs(endpoint - flank_coordinate) <= clearance:` | `->L1996` |  |
| 2053 | `if endpoint is None:` | `->L2054` |  |
| 2063 | `if coordinate is None:` | `->L2064` |  |
| 2066 | `not forked` | `->L2067`, `->L2070` |  |
| 2067 | `or landing.member_id == landing_plan.primary_trunk_member_id` | `->L2068`, `->L2070` |  |
| 2068 | `or landing.opening_turn_coordinate is None` | `->L2070`, `->L2071` |  |
| 2074 | `if reseated is None:` | `->L2075`, `->L2076` |  |
| 2119 | `if not (` | `->L2122` |  |
| 2138 | `if axis is None or axis.axis is not DemandAxis.X:` | `->L2139` |  |
| 2194 | `if primary_edge is not None and plan.primary_trunk_member_id is not None:` | `->L2181` |  |
| 2365 | `if endpoint is None or abs(endpoint - column) <= curve_radius:` | `->L2366` |  |
| 2383 | `if (endpoint - candidate) * toward_endpoint <= curve_radius:` | `->L2384` |  |
| 2406 | `gap_right <= gap_left` | `->L2410` |  |
| 2455 | `if axis is None:` | `->L2456` |  |
| 2508 | `if axis is None or axis.axis is not DemandAxis.X or primary is None:` | `->L2509` |  |
| 2583 | `if plan.trunk_axis is None or plan.trunk_axis.axis is not DemandAxis.X:` | `->L2584` |  |
| 2716 | `if not landing_feasible(candidate_coordinate):` | `->L2717` |  |
| 2726 | `if (` | `->L2730` |  |
| 2756 | `if not obstacles:` | `->L2758` |  |
| 2778 | `for coordinate in candidates:` | `->L2743`, `->L2779` |  |
| 2779 | `if not (` | `->L2784`, `->L2785` |  |
| 2791 | `if moved_plan is None:` | `->L2792`, `->L2793` |  |
| 2802 | `if moved_channel is None or any(` | `->L2806`, `->L2807` |  |
| 2811 | `if (` | `->L2815`, `->L2816` |  |
| 2876 | `if (` | `->L2880` |  |
| 2892 | `if first.system_id != second.system_id:` | `->L2893` |  |
| 3045 | `if conflict is not None:` | `->L3046` |  |
| 3070 | `if shared_carrier and separation > COORD_TOLERANCE:` | `->L3071` |  |
| 3077 | `if (` | `->L3088` |  |
| 3095 | `if _gap_channels_crowd(` | `->L3102` |  |
| 3117 | `if not plan.owns_geometry:` | `->L3118` |  |
| 3181 | `if not plan.owns_geometry:` | `->L3182` |  |
| 3204 | `owner_edge not in edge_ranks` | `->L3208` |  |
| 3242 | `if not plan.owns_geometry:` | `->L3243` |  |
| 3261 | `if ownership.edge in by_edge:` | `->L3262` |  |
| 3375 | `if conflict is not None:` | `->L3376` |  |
| 3383 | `for item in system_plans:` | `->L3384`, `->L3393` |  |
| 3632 | `if not horizontal and not vertical:` | `->L3633` |  |
| 3656 | `if interval[1] - interval[0] > COORD_TOLERANCE:` | `->L3641` |  |
| 3659 | `for interval_start, interval_end in sorted(intervals):` | `->L3667` |  |
| 3662 | `if interval_start > covered_until + COORD_TOLERANCE:` | `->L3663` |  |
| 3665 | `if covered_until >= extent_end - COORD_TOLERANCE:` | `->L3659` |  |
| 3688 | `if axis.axis is DemandAxis.X:` | `->L3711` |  |
| 3808 | `if not candidates:` | `->L3809` |  |
| 3893 | `if not 0 <= flank_rank < len(route.points) - 1:` | `->L3894` |  |
| 3906 | `if actual is None:` | `->L3907` |  |
| 3913 | `direction is not landing.approach_direction` | `->L3917` |  |
| 3914 | `or handedness is not landing.corner_handedness` | `->L3917` |  |
| 3915 | `or runway < landing.minimum_runway - COORD_TOLERANCE` | `->L3917` |  |
| 3934 | `opening is None` | `->L3938` |  |
| 3935 | `or abs(opening.x - landing.opening_turn_coordinate) > COORD_TOLERANCE` | `->L3938` |  |
| 3936 | `or emitted_segment != landing.opening_turn_segment` | `->L3938` |  |
| 3947 | `if query is None:` | `->L3948` |  |
| 3953 | `if not plan.owns_geometry:` | `->L3954` |  |
| 3958 | `if landing is None:` | `->L3959` |  |
| 3960 | `if continuation is not None and continuation.covered_by_member_id is not None:` | `->L3961`, `->L3962` |  |
| 3962 | `if continuation is not None and (` | `->L3972`, `->L3976` |  |
| 3976 | `if plan.primary_trunk_member_id == membership.member_id:` | `->L3977`, `->L3981` |  |
| 3999 | `if opening is None:` | `->L4000` |  |
| 4022 | `if ctx.validate_final_route_frames:` | `->L4024` |  |
| 4039 | `if any(` | `->L4043` |  |
| 4044 | `if ctx.validate_final_route_frames:` | `->L-3945` |  |
| 4058 | `if not plan.owns_geometry:` | `->L4059` |  |
| 4067 | `if trunk_route is None or not _route_covers_trunk(trunk_route, plan.trunk_axis):` | `->L4068` |  |
| 4077 | `if route is None:` | `->L4078` |  |
| 4083 | `if (` | `->L4088` |  |
| 4095 | `if any(` | `->L4099` |  |
| 4116 | `if continuation.covered_by_member_id is not None:` | `->L4133` |  |
| 4119 | `if (` | `->L4128` |  |
| 4134 | `if (` | `->L4113`, `->L4145` |  |
| 4161 | `if route is None or membership is None:` | `->L4162` |  |
| 4167 | `if any(` | `->L4171` |  |

## `core.py`

23/31 gates fully exercised.

Gates with an un-exercised arm:

| Line | Gate | Un-exercised arm(s) | Triage |
| ---: | --- | --- | --- |
| 194 | `if scaffold is None:` | `->L201` |  |
| 216 | `if rail_execution is not None:` | `->L220` |  |
| 394 | `if result is not None:` | `->L-344` | **defensive** -- Append-guard after the priority-ordered handler chain. The final handler _route_intra_section is total: its three early returns and its _route_diagonal fallback (annotated -> RoutedPath, single return statement) always yield a RoutedPath, so result is never None and every edge appends. The None arm is unreachable. |
| 395 | `if system_execution is not None:` | `->L397` |  |
| 415 | `if system.system_id != expected.system_id:` | `->L416` |  |
| 439 | `if system_execution is not None and next_system_rank != len(` | `->L442` |  |
| 558 | `if system_execution is not None:` | `->L567` |  |
| 583 | `if validate_final_route_frames:` | `->L585` |  |

## `corners.py`

8/10 gates fully exercised.

Gates with an un-exercised arm:

| Line | Gate | Un-exercised arm(s) | Triage |
| ---: | --- | --- | --- |
| 50 | `if corner_idx < 0 or corner_idx >= n_corners:` | `->L51` | **defensive** -- Bounds check for resolve_curve_radius_at. Production callers use indices from range(n_corners) or recorded corner buckets, so an invalid index requires a caller-contract violation and is covered as an error case by the corner unit tests. |
| 509 | `if vertical is Direction.D:` | `->L511`, `->L516` |  |

## `dispatch.py`

2/3 gates fully exercised.

Gates with an un-exercised arm:

| Line | Gate | Un-exercised arm(s) | Triage |
| ---: | --- | --- | --- |
| 53 | `if route is not None and observe_fallback:` | `->L54` |  |

## `exit_turns.py`

137/214 gates fully exercised.

Gates with an un-exercised arm:

| Line | Gate | Un-exercised arm(s) | Triage |
| ---: | --- | --- | --- |
| 427 | `if key not in ctx.station_offsets and port_key not in ctx.station_offsets:` | `->L428` |  |
| 441 | `if len({coordinate for _line, _offset, coordinate in values}) != len(values):` | `->L442` |  |
| 465 | `if source_run_direction not in {Direction.U, Direction.D}:` | `->L466` |  |
| 495 | `if source_run_direction not in {Direction.R, Direction.L}:` | `->L496` |  |
| 504 | `if abs(tgt.x - src.x) <= COORD_TOLERANCE:` | `->L505` |  |
| 514 | `if actual_run is not source_run_direction:` | `->L515` |  |
| 599 | `if kind is _MergeEntryRoute.STRAIGHT:` | `->L600` |  |
| 663 | `if abs(turn_delta) <= COORD_TOLERANCE:` | `->L664` |  |
| 775 | `if primary_delta * run_direction.sign < 2 * MIN_STRAIGHT_EDGE + diagonal:` | `->L776` |  |
| 802 | `if direction_axis(transition.run_direction) is DemandAxis.X:` | `->L806` |  |
| 855 | `if not mismatched:` | `->L857` |  |
| 857 | `if len(candidates) != 1:` | `->L858`, `->L859` |  |
| 864 | `if direction_axis(run_direction) is DemandAxis.Y:` | `->L865`, `->L881` |  |
| 892 | `if transition is None:` | `->L893`, `->L894` |  |
| 929 | `target_crossing is None` | `->L932` |  |
| 930 | `or abs(source_crossing - target_crossing) > COORD_TOLERANCE` | `->L932` |  |
| 942 | `if direction_axis(run_direction) is DemandAxis.Y:` | `->L943` |  |
| 968 | `if transition is None:` | `->L969` |  |
| 1002 | `if direction_axis(run_direction) is DemandAxis.Y:` | `->L1003` |  |
| 1028 | `if transition is None:` | `->L1030` |  |
| 1061 | `if len(entry_group_ids) != 1:` | `->L1062` |  |
| 1068 | `if family_id is None:` | `->L1069` |  |
| 1092 | `and requirement.turn_direction is not None` | `->L1099` |  |
| 1345 | `if reason is not None:` | `->L1346` |  |
| 1368 | `if not _lane_transition_order_is_preserved(transitions):` | `->L1369` |  |
| 1434 | `if not outbound_edges or missing_connectors:` | `->L1435` |  |
| 1523 | `if final_classification.legacy_reason is not None:` | `->L1524` |  |
| 1525 | `elif tuple(seed.family_id for seed in final_classification.seeds) != tuple(` | `->L1528` |  |
| 1787 | `if incumbent.id == plan.id:` | `->L1789` |  |
| 1857 | `if owners and plan.id not in owners:` | `->L1858` |  |
| 1858 | `for owner in owners:` | `->L1859`, `->L1860` |  |
| 1873 | `and plan.id in conflicting_plan_ids` | `->L1875` |  |
| 1891 | `and axis.fixed_anchor_offset is not None` | `->L1880` |  |
| 1892 | `and abs(proposed - axis.fixed_anchor_offset) > COORD_TOLERANCE` | `->L1894` |  |
| 2018 | `if abs(first_offset - second_offset) > COORD_TOLERANCE:` | `->L2020` |  |
| 2020 | `for owner in (first_owner, second_owner):` | `->L2017`, `->L2021` |  |
| 2021 | `if owner is not None:` | `->L2020`, `->L2022` |  |
| 2028 | `if not additions:` | `->L2030` |  |
| 2068 | `if assignment.planned_family_id is not RouteFamilyId.TB_BOTTOM_EXIT:` | `->L2069` |  |
| 2078 | `if geometry.bundle_offsets is None:` | `->L2079` |  |
| 2166 | `if scaffold is None:` | `->L2167` |  |
| 2174 | `if scaffold is None:` | `->L2175` | **defensive** -- The second None check protects the manual or incomplete graph fallback after rebuilding a missing retained scaffold. Parsed render-path graphs publish one canonical scaffold during fan planning, so the fixture corpus never reaches a rebuild that can return None. Direct planner tests retain the fallback for graph construction without topology. |
| 2267 | `if transition_key in transition_by_edge:` | `->L2268` |  |
| 2294 | `if abs(dx) <= COORD_TOLERANCE and abs(dy) > COORD_TOLERANCE:` | `->L2296` |  |
| 2303 | `if len(points) >= 3:` | `->L2310` |  |
| 2306 | `_segment_direction(before, start) is run_direction` | `->L2310` |  |
| 2307 | `and _segment_direction(start, end) is turn_direction` | `->L2310` |  |
| 2351 | `if ctx.exit_turns is None:` | `->L2352` |  |
| 2362 | `if family_id is not assignment.planned_family_id:` | `->L2363` |  |
| 2375 | `if (` | `->L2384` |  |
| 2393 | `run is None` | `->L2398` |  |
| 2394 | `or turn is None` | `->L2398` |  |
| 2395 | `or assignment.launch_coordinate is None` | `->L2398` |  |
| 2396 | `or assignment.minimum_runway is None` | `->L2398` |  |
| 2402 | `if segment_rank is None:` | `->L2403` |  |
| 2450 | `if (` | `->L2462` |  |
| 2489 | `if rank is None:` | `->L2490` |  |
| 2551 | `if current.keys() != snapshot.geometry.keys():` | `->L2552` |  |
| 2566 | `if current[key] != state:` | `->L2567` |  |
| 2592 | `if isinstance(plan, RoutePlan):` | `->L2593` |  |
| 2611 | `station_id not in graph.stations` | `->L2614` |  |
| 2612 | `or lane.line_id not in graph.station_lines(station_id)` | `->L2614` |  |
| 2620 | `if (station_id, lane.line_id) not in station_offsets or (` | `->L2627` |  |
| 2635 | `if len(transition_routes) != 1:` | `->L2636` |  |
| 2673 | `route.exit_lane_transition_plan_id != str(exit_turn_plan.id)` | `->L2678` |  |
| 2674 | `or route.offset_regime is not OffsetRegime.BAKED` | `->L2678` |  |
| 2675 | `or route.points != expected_route.points` | `->L2678` |  |
| 2676 | `or route.curve_radii != expected_route.curve_radii` | `->L2678` |  |
| 2694 | `if not _fixed_axis_matches_plan(` | `->L2699` |  |
| 2705 | `if any(` | `->L2714` |  |
| 2722 | `if len(member_routes) != 1:` | `->L2723` |  |
| 2730 | `if route.exit_turn_family_id != assignment.planned_family_id.value:` | `->L2731` |  |
| 2738 | `if route.exit_turn_axis_id is not None:` | `->L2739` |  |
| 2747 | `if (` | `->L2761` |  |
| 2769 | `route.exit_turn_axis_id != str(assignment.axis_id)` | `->L2772` |  |
| 2770 | `or route.exit_turn_segment_rank is None` | `->L2772` |  |
| 2783 | `if (` | `->L2805` |  |

## `inter_section.py`

0/4 gates fully exercised.

Gates with an un-exercised arm:

| Line | Gate | Un-exercised arm(s) | Triage |
| ---: | --- | --- | --- |
| 98 | `if pair not in _CW_TURNS and pair not in _CCW_TURNS:` | `->L-96`, `->L99` | **defensive** -- Corner.__post_init__ turn-validity check. inter_section.py is the descriptor/documentation module (distinct from inter_section_handlers.py); no src module imports it, so it never loads during the render-corpus sweep (confirmed absent from sys.modules after rendering the whole corpus) and both arms show 0 hits. Corner instances are module-level constants built from valid right-angle turns, so the raise can only fire on a source edit. Exercised by test_inter_section_descriptor.py. |
| 107 | `if (self.in_tangent, self.out_tangent) in _CW_TURNS:` | `->L108`, `->L109` | **defensive** -- Corner.handedness property. Same module-load story: inter_section.py is never imported on the render path, so neither the CW nor the CCW arm is reachable by any render topology. Both branches are exercised by test_inter_section_descriptor.py. |
| 155 | `for prev, curr in zip(self.corners, self.corners[1:]):` | `->L156`, `->L158` | **defensive** -- TurnSequence.parity loop over consecutive corners. parity is descriptor-level documentation (runtime wrap handlers preserve bundle ordering via per-corner offset propagation regardless of this value); nothing on the render path reads it, and the module is never imported during the sweep. Exercised by test_inter_section_descriptor.py. |
| 156 | `if prev.handedness != curr.handedness:` | `->L155`, `->L157` | **defensive** -- Handedness-change counter inside TurnSequence.parity. Off the render path for the same reason (inter_section.py never loaded during rendering); both arms are exercised by test_inter_section_descriptor.py's parity-contract tests. |

## `inter_section_handlers.py`

157/300 gates fully exercised.

Gates with an un-exercised arm:

| Line | Gate | Un-exercised arm(s) | Triage |
| ---: | --- | --- | --- |
| 336 | `if section is None:` | `->L337` |  |
| 547 | `and f.src_row is not None` | `->L578` | **defensive** -- LEFT-entry inter-row L-shape guard. Every LEFT-entry inter-section edge reaching this condition has a resolved source grid row (f.src_col/f.tgt_col are asserted non-None just above and the row resolves alongside), so the `f.src_row is None` short-circuit out of the L-shape branch is never taken. |
| 548 | `and f.tgt_row is not None` | `->L578` | **defensive** -- Companion guard to f.src_row in the same LEFT-entry inter-row L-shape condition: the target grid row is likewise always resolved here, so the `f.tgt_row is None` short-circuit out of the branch is never taken. |
| 592 | `if geometry is not None:` | `->L593` |  |
| 613 | `if src_sec is None:` | `->L614` |  |
| 617 | `if edges is None:` | `->L618` |  |
| 632 | `if section and section.bbox_w > 0:` | `->L634` |  |
| 640 | `if section and section.bbox_w > 0:` | `->L642` |  |
| 666 | `if _v_segment_crosses_other_section(graph, corner_x, src.y, ey, exclude):` | `->L667` | **defensive** -- Drop-in viability test in _right_entry_drop_in_is_clear (#889). The cross-row RIGHT-entry rule fires only when the earlier bypass / plough rules ceded the edge, i.e. no section sits between source and port, so the outward-side descent runs clear; the not-clear arm covers only an exotic descent blocked by a wide same-column sibling no corpus fixture builds. Defensive. |
| 772 | `if min(drop_xs) < _section_right_edge(graph, tgt) - COORD_TOLERANCE:` | `->L773` |  |
| 775 | `if _v_segment_crosses_other_section(graph, dx, src.y, tgt.y, exclude):` | `->L776` |  |
| 816 | `if route is not None:` | `->L818` |  |
| 948 | `if route is not None:` | `->L951` |  |
| 1069 | `if _corridor_is_viable(ctx, src, tgt):` | `->L1070` | **needs-review** -- Reachable only via a defective render (inter-row corridor grazes the source section); see #722. |
| 1102 | `if target_port is None:` | `->L1103` |  |
| 1121 | `if sibling_port is None or not sibling_port.is_entry:` | `->L1122` |  |
| 1125 | `if (` | `->L1133` |  |
| 1135 | `if not candidates:` | `->L1136` |  |
| 1147 | `if (` | `->L1153` |  |
| 1164 | `if sibling_route is None or len(sibling_route.points) < 6:` | `->L1165` |  |
| 1176 | `if (` | `->L1185` |  |
| 1227 | `if (` | `->L1235` |  |
| 1311 | `if abs(ep.y - f.sy) < ctx.curve_radius:` | `->L1312` | **defensive** -- Merge entry-port Y tracks the source/junction Y, so a sub-curve_radius exit->entry gap never arises (the same-Y rule already handles dy<tol); corpus instrumentation across the fixtures (#762) confirms the arm is never taken. Defensive. |
| 1314 | `if ep_port and ep_port.side in (PortSide.TOP, PortSide.BOTTOM):` | `->L1315` |  |
| 1316 | `if ep_port and ep_port.side == PortSide.LEFT:` | `->L1322` | **defensive** -- Contract guard in _route_inter_section: a merge entry port always resolves to a Port on the LEFT side here, so the non-LEFT/None arm (fall through to _route_l_shape at L344) is never taken (0/128). The earlier 'multiline phantom' note was inaccurate -- this is a recorded gate whose arm is never taken. Defensive guard. Reclassified defensive (#762). |
| 1319 | `if _corridor_is_viable(ctx, src, ep):` | `->L1321` | **needs-review** -- Reachable only via a defective render (route skirts/crosses section boundary); see #724. |
| 1343 | `if tgt_sec is None:` | `->L1344`, `->L1345` | **defensive** -- Defensive guard in _fan_shares_inter_row_channel: the edge is a fan branch into an entry port, which always resolves to a section, so the None arm is unreachable in practice. |
| 1345 | `if port.side is PortSide.TOP:` | `->L1346`, `->L1351` |  |
| 1626 | `if len(_INTER_SECTION_RULE_BY_FAMILY) != len(_INDEXED_INTER_SECTION_RULES):` | `->L1627`, `->L1630` |  |
| 1632 | `if _INTER_SECTION_RULES is _INDEXED_INTER_SECTION_RULES:` | `->L1634` |  |
| 1674 | `if family_id is not RouteFamilyId.STANDARD_L_SHAPE:` | `->L1675` |  |
| 1688 | `route is None` | `->L1689` |  |
| 1689 | `and membership is not None` | `->L1690`, `->L1699` |  |
| 1690 | `and membership.assignment is not None` | `->L1691`, `->L1699` |  |
| 1691 | `and membership.plan.disposition is ExitTurnDisposition.PLANNED` | `->L1693`, `->L1699` |  |
| 1699 | `if route is not None:` | `->L1704` |  |
| 1720 | `if not is_inter:` | `->L1721` |  |
| 1855 | `if planned_transition is not None:` | `->L1856` |  |
| 2051 | `if ctx.station_offsets:` | `->L2053` | **defensive** -- _run_pass_c_guards always populates ctx.station_offsets before route_edges, and RAILS mode returns earlier; the empty-offsets arm never fires. Reclassified candidate-dead -> defensive (#762): corpus instrumentation across 128 fixtures confirms the un-exercised arm is never taken. |
| 2079 | `if detour is not None:` | `->L2082` |  |
| 2129 | `if query is None:` | `->L2130` | **defensive** -- Production routing runs after layout publishes FanPlanExecution. The None arm preserves the ordinary dispatcher for direct or incomplete routing contexts. |
| 2138 | `emission.emitter is not FanRouteEmitter.BOTTOM_EXIT_RIGHT_LANDINGS` | `->L2143` | **defensive** -- FanPlanExecution returns the frozen emission selected by the planner, and BOTTOM_EXIT_RIGHT_LANDINGS is the only FanRouteEmitter member. The mismatch arm detects a corrupted or incompatible plan. |
| 2139 | `or plan.fork_station_id != edge.source` | `->L2143` | **defensive** -- FanPlan membership validation requires every emission source to equal fork_station_id. This operand catches a frozen-plan/source mismatch before materialisation. |
| 2140 | `or target_port is None` | `->L2143` | **defensive** -- The planner creates this emitter only for a resolved landing port. A missing target port therefore indicates graph or plan drift after publication. |
| 2141 | `or target_port.side is not PortSide.RIGHT` | `->L2143` | **defensive** -- BOTTOM_EXIT_RIGHT_LANDINGS is selected only when every target is a RIGHT entry. The non-RIGHT arm is a defensive check that the frozen target still satisfies that contract. |
| 2152 | `if port is None or port.side is not PortSide.RIGHT or section is None:` | `->L2153` | **defensive** -- The planned emitter is created only from existing RIGHT landing ports with owning sections. This guard rejects stale or corrupted port references during materialisation. |
| 2156 | `if section not in landing_sections:` | `->L2147` | **defensive** -- The planner admits this emitter only when branches land in distinct sections, so the duplicate-section arm cannot occur for a valid frozen emission. It remains a materialisation-time drift check. |
| 2158 | `if len(landing_sections) != len(plan.branches):` | `->L2159` | **defensive** -- The planner emits this route family only when every branch lands in one distinct RIGHT-entry section. A count mismatch can arise only if the frozen emission and plan have drifted apart. |
| 2161 | `if plan.entry_runway is None:` | `->L2162` | **defensive** -- Every PLANNED FanPlan is validated with both runways before publication. The None arm is a runtime drift check for an incomplete plan object. |
| 2165 | `if not source_lines or edge.line_id not in source_lines:` | `->L2166` | **defensive** -- The planned emitter requires a non-empty offset_line_order whose line set exactly matches its emissions. The missing-line arm protects routing from a malformed frozen plan. |
| 2192 | `if route is None:` | `->L2193` |  |
| 2223 | `if tgt_port is None or tgt_port.side != PortSide.LEFT:` | `->L2224` |  |
| 2226 | `if ep_col is None or ep_row is None:` | `->L2227` |  |
| 2229 | `if corner_x is None:` | `->L2230` |  |
| 2356 | `if _would_route_around_section_below(other, ctx):` | `->L2357` | **defensive** -- Competing-sibling detection in _has_around_section_sibling, routed through the dispatch table (#813). A non-bypass sibling feeding the same merge junction sits in an adjacent column or a gap with no same-row intervening section, so it dispatches to an L-shape, not the around-below loop; corpus instrumentation across the fixtures confirms the arm is never taken. Defensive. |
| 2606 | `and corridor.bypass_band_y >= base_y - COORD_TOLERANCE` | `->L2615` | **needs-review** -- Safety fallback: the shared bypass band is the deepest sibling's bypass_bottom_y, so a branch's own base_y never exceeds it; the arm that would decline a shallower shared band (guarding a predicate mismatch) is unexercised in the corpus. |
| 2678 | `if trunk_v_up_pull_away:` | `->L2687` | **needs-review** -- Reachable only via a defective render (merge-junction tangle); see #721. |
| 2717 | `this_xmin - gap_left >= SECTION_ROUTE_CLEARANCE` | `->L2718`, `->L2721` |  |
| 2718 | `and gap_right - around_xmax >= SECTION_ROUTE_CLEARANCE` | `->L2720`, `->L2721` |  |
| 2793 | `if src_sec is not None and src_sec.bbox_w > 0:` | `->L2843` | **defensive** -- A bypass source section always resolves and has bbox_w>0 (the parser never emits an empty section); the None/zero-width arm never fires. |
| 2905 | `if route is None:` | `->L2906` | **defensive** -- Defensive in _declare_trunk: _route_inter_section only reaches the declaration after a matched rule (or the L-shape fall-through) produced a route, so the None guard never fires on the corpus; it mirrors _declare_channel's same guard for a handler that returns None. |
| 2933 | `if route is None:` | `->L2934` | **defensive** -- _declare_channel deliberately accepts RoutedPath \| None so declining builders can be ignored. Production callers currently pass concrete routes; the None no-op remains a defensive helper contract. |
| 3155 | `while cur is not None and cur not in seen:` | `->L3171` |  |
| 3164 | `if cur in graph.junctions:` | `->L3170` |  |
| 3212 | `if (` | `->L3218` |  |
| 3421 | `if abs(tgt.x - src.x) <= ctx.curve_radius:` | `->L3422` |  |
| 3434 | `if _h_segment_crosses_other_section(graph, src.x, final_x, src.y, exclude):` | `->L3435` | **defensive** -- Traverse-first clearance test in _top_entry_side_fan_traverse_clears (#1349). The below-side fan branch only reaches this gate when its junction also feeds an aligned straight drop; the corpus fan sits a clear row above its target, so the source-Y traverse never crosses another section and the fall-back-to-staircase arm is not exercised. Defensive. |
| 3597 | `if feeder is not None:` | `->L3602` |  |
| 3825 | `if mid_y is None:` | `->L3847` |  |
| 3862 | `channel_y is None` | `->L3888` |  |
| 3864 | `and src_sec is not None` | `->L3888` | **defensive** -- Inter-row exit mid-Y clearance bump (the n>1 multi-line branch). When more than one line shares the exit the source section always resolves, so the `src_sec is None` short-circuit past the clearance bump is never taken. |
| 3865 | `and tgt_sec is not None` | `->L3888` | **defensive** -- Companion to src_sec in the same n>1 mid-Y clearance branch: the target section always resolves there, so the `tgt_sec is None` short-circuit past the bump is never taken. |
| 3892 | `elif abs(dx) > ctx.curve_radius:` | `->L3893` |  |
| 3896 | `if src.id in ctx.graph.junctions:` | `->L3906` | **needs-review** -- Reachable only via a defective render (LR->TB TOP-entry routing); see #720. |
| 3897 | `for je in ctx.graph.edges_to(src.id):` | `->L3906` |  |
| 3899 | `if js.is_port:` | `->L3897` |  |
| 3900 | `if abs(js.x - src.x) <= COORD_TOLERANCE:` | `->L3903` |  |
| 3919 | `if exit_side is not None and not straight_drop:` | `->L3929` |  |
| 3965 | `if not straight_drop:` | `->L3970` | **needs-review** -- A top-entry fan branch fed straight from directly above the junction (straight_drop) keeps its lead-in column at the source X rather than the shared fan pivot; the corpus's fan branches all carry horizontal travel, so the straight-drop arm is unexercised. |
| 3967 | `if lead is Direction.R:` | `->L3970` | **needs-review** -- A top-entry fan branch leading LEFT out of the junction skips the _v1_corner_x right-edge clearance bump; the corpus's fan branches all lead right, so the left-lead arm is unexercised. |
| 4040 | `if src_sec is None or tgt_sec is None or src_sec.grid_row >= tgt_sec.grid_row:` | `->L4043` |  |
| 4098 | `if mid_y is None:` | `->L4114` |  |
| 4129 | `channel_y is None` | `->L4155` |  |
| 4131 | `and src_sec is not None` | `->L4155` |  |
| 4132 | `and tgt_sec is not None` | `->L4155` |  |
| 4133 | `and src_sec.grid_row != tgt_sec.grid_row` | `->L4135` |  |
| 4157 | `if exit_side is not None:` | `->L4159` |  |
| 4159 | `elif abs(dx) > ctx.curve_radius:` | `->L4160`, `->L4162` |  |
| 4163 | `if src.id in ctx.graph.junctions:` | `->L4164`, `->L4173` |  |
| 4164 | `for je in ctx.graph.edges_to(src.id):` | `->L4165`, `->L4173` |  |
| 4166 | `if js.is_port:` | `->L4164`, `->L4167` |  |
| 4167 | `if abs(js.x - src.x) <= COORD_TOLERANCE:` | `->L4168`, `->L4170` |  |
| 4186 | `if exit_side is not None and not straight_drop:` | `->L4196` |  |
| 4188 | `if corridor_x is not None:` | `->L4196` |  |
| 4225 | `if fan_single is not None:` | `->L4226` |  |
| 4228 | `if channel_y is None and corridor is not None and corridor.band_y is not None:` | `->L4231`, `->L4232` |  |
| 4232 | `if not straight_drop:` | `->L4233`, `->L4237` |  |
| 4234 | `if lead is Direction.R:` | `->L4235`, `->L4237` |  |
| 4350 | `if src_section and src_section.bbox_w > 0:` | `->L4351` |  |
| 4371 | `if tgt_sec is None:` | `->L4372` |  |
| 4431 | `if src_col is None:` | `->L4432` |  |
| 4623 | `if src_sec is not None:` | `->L4654` | **defensive** -- Defensive guard in the fanned entry-wrap source-clearance lift: a fan-out junction always resolves to its source section, so the None arm is unreachable in practice. |
| 4660 | `if tgt_col is not None:` | `->L4665` | **defensive** -- tgt_col from _resolve_section_col on a valid LEFT entry port is always non-None (sections get grid_col>=0 after auto-layout). |
| 4662 | `if shared_vx is not None:` | `->L4665` | **defensive** -- _fan_left_entry_descent_x returns None only when col_left<=0; col_left_edge for the target's own column is always >0. |
| 4722 | `if entry_side is PortSide.LEFT:` | `->L4725` |  |
| 4757 | `if entry_port is None:` | `->L4758` | **defensive** -- _route_around_section_below is always called with a non-None entry_port (both dispatch sites pass a checked station). |
| 4768 | `if trunk_src is None or trunk_src == edge.source:` | `->L4770` | **needs-review** -- Reachable only via a defective render (merge-junction tangle); see #721. |
| 4821 | `if col_left <= 0.0:` | `->L4822` | **defensive** -- col_left_edge's 0.0 default only fires for a column with no sections; the target's own column always holds its section. |
| 4858 | `if entry_port is None:` | `->L4859` | **defensive** -- Every caller of _corridor_is_viable passes a non-None entry_port. |
| 4865 | `if src_row is None or ep_row is None or src_col is None or ep_col is None:` | `->L4866` | **defensive** -- _resolve_section_colrow returns (None,None) only for a station with no section; valid entry ports always have a section with non-negative grid coords. |
| 4953 | `if fan is not None and corridor is not None and corridor.band_y is not None:` | `->L4957` | **needs-review** -- Corridor-feeder shared inter-row band gate: the corpus's corridor feeders belong to fans whose corridor carries a band_y, so the short-circuit past it (no corridor or bypass-only) is unexercised; it falls to the elif fan-is-not-None global-band branch. |
| 4957 | `elif fan is not None:` | `->L4961`, `->L4966` | **needs-review** -- Corridor-feeder fallback for a fan whose junction earned no corridor (its in-column gap below does not fit the bundle); the corpus's fans all earn a corridor, so this global-band branch is unexercised. |
| 4966 | `elif gap_bottom > gap_top:` | `->L4967`, `->L4971` | **needs-review** -- Reachable only via a defective render (inter-row corridor on a <78px gap); see #722. |
| 4976 | `if fan is None and gap_bottom > gap_top:` | `->L4977` | **needs-review** -- Reachable only via a defective render (inter-row corridor on a <78px gap); see #722. |
| 4990 | `if fan is not None and ep_col is not None:` | `->L4992` | **needs-review** -- Reachable only via a defective render (inter-row corridor on a <78px gap); see #722. |
| 4992 | `if vx is None:` | `->L4993` | **needs-review** -- Reachable only via a defective render (inter-row corridor on a <78px gap); see #722. |
| 5105 | `if ep_section and ep_section.bbox_w > 0:` | `->L5108` | **defensive** -- entry_port.section_id is always set and the section has bbox_w>0 after layout; the else arm never fires. |
| 5272 | `if min(hi, seg_hi) - max(lo, seg_lo) > COORD_TOLERANCE:` | `->L5273` |  |
| 5274 | `if ctx.convergences is None:` | `->L5275` |  |
| 5276 | `for claim in ctx.convergences.prior_vertical_channels_for_edge(edge):` | `->L5277` |  |
| 5277 | `if claim.line_id != edge.line_id or claim.owner_source == edge.source:` | `->L5278`, `->L5279` |  |
| 5279 | `if not (corner_x - COORD_TOLERANCE <= claim.x <= gap_right + COORD_TOLERANCE):` | `->L5280`, `->L5281` |  |
| 5281 | `if min(hi, claim.y_hi) - max(lo, claim.y_lo) > COORD_TOLERANCE:` | `->L5276`, `->L5282` |  |
| 5348 | `if _leadout_self_meets_sibling_descent(ctx, edge, corner_x, sy, hy, gap_right):` | `->L5349` |  |
| 5426 | `if gap_bottom <= gap_top:` | `->L5427` | **defensive** -- The row-gap enforcer keeps stacked rows separated, so the inter-row band above a target row (row_bottom_edge(tgt_row-1) up to row_top_edge(tgt_row)) has positive height whenever a RIGHT entry is fed from a higher row; the degenerate-band arm guards an abutting-rows layout placement never produces. |
| 5430 | `if not _inter_row_band_fits(gap_top, gap_bottom):` | `->L5431` | **defensive** -- Band-fit test in _right_entry_gap_above_is_clear (#889), reached only when the cross-row RIGHT-entry drop-in is not clear (the exotic blocked-descent case above). The corpus always takes the drop-in, so neither arm of the gap-above fallback is exercised. Defensive. |
| 5510 | `if not normalize_exempt:` | `->L5514` |  |
| 5587 | `if gap_bottom <= gap_top:` | `->L5588` |  |
| 5589 | `if not _inter_row_band_fits(gap_top, gap_bottom):` | `->L5590` |  |
| 5601 | `if _v_segment_crosses_other_section(graph, vx, gy, tgt.y, exclude):` | `->L5602` |  |
| 5685 | `if source is None or source.bbox_w <= 0:` | `->L5686` |  |
| 5690 | `if source.id not in members:` | `->L5691` |  |
| 5696 | `if other.bbox_x <= source_right + COORD_TOLERANCE:` | `->L5697` |  |
| 5698 | `if cellmate_left is None or other.bbox_x < cellmate_left:` | `->L5692` |  |
| 5700 | `if cellmate_left is None:` | `->L5701` |  |
| 5738 | `if band0_bottom <= band0_top or not _inter_row_band_fits(band0_top, band0_bottom):` | `->L5739` |  |
| 5740 | `if band1_bottom <= band1_top or not _inter_row_band_fits(band1_top, band1_bottom):` | `->L5741` |  |
| 5742 | `if band1_top <= band0_bottom + COORD_TOLERANCE:` | `->L5743` |  |
| 5758 | `if _v_segment_crosses_other_section(graph, corner_x, band0_y, band1_y, exclude):` | `->L5759` |  |
| 5793 | `if geom is None:` | `->L5794` |  |
| 5797 | `if _v_segment_crosses_other_section(` | `->L5800` |  |
| 5801 | `if _h_segment_crosses_other_section(` | `->L5804` |  |
| 5805 | `if _h_segment_crosses_other_section(` | `->L5808` |  |

## `intra_handlers.py`

47/72 gates fully exercised.

Gates with an un-exercised arm:

| Line | Gate | Un-exercised arm(s) | Triage |
| ---: | --- | --- | --- |
| 82 | `if not section:` | `->L83` | **defensive** -- _route_entry_runway only runs on an entry-port -> internal-station edge; the target of such an edge always carries a section_id that resolves, so the guard's return-None arm protects against a malformed graph rather than a topology. |
| 89 | `if section.direction == "RL" and port.side != PortSide.RIGHT:` | `->L90` |  |
| 91 | `if section.direction not in ("LR", "RL"):` | `->L92` | **defensive** -- No TB/BT entry reaches _route_entry_runway: TB LEFT/RIGHT entries are consumed by _route_tb_lr_entry and TB/BT TOP/BOTTOM entries by _route_perp_entry (both earlier in the dispatch chain), and a BT section is never produced (the direction directive accepts only LR/RL/TB; 0/391 corpus sections are BT). Reclassified candidate-dead -> defensive (#762): corpus instrumentation across 128 fixtures confirms the un-exercised arm is never taken. |
| 137 | `if abs(nearest_src - sx) < src_min + ctx.diagonal_run:` | `->L138` | **defensive** -- Entry-runway 'too tight' return cannot fire: \|nearest_src - entry_port_x\| is floored at SECTION_X_PADDING (50px, the entry port sits at section.bbox_x = min(internal_station_x) - SECTION_X_PADDING), while src_min + diagonal_run = CURVE_RADIUS(10) + MIN_STRAIGHT_PORT(5) + DIAGONAL_RUN(30) = 45px. Since 50 >= 45 the guard is always False. |
| 148 | `elif sy_blocked:` | `->L149` | **defensive** -- Entry-runway trunk-row non-consumer bypass (#1293). Both-rows-blocked fall-through: a non-consumer sits on BOTH the entry row and the target trunk row, leaving no clear runway row. No corpus fixture has a doubly-blocked flow-side entry; defensive. |
| 160 | `if abs(diag_start_x - sx) <= abs(last_blocker - sx):` | `->L161` | **defensive** -- Entry-runway trunk-row non-consumer bypass (#1293). Descent-clips-blocker fall-through: the join-biased diagonal cannot start past the furthest target-row non-consumer (a blocker within diagonal_run+tgt_min of the target). Corpus blockers sit far enough forward; defensive. |
| 189 | `for k in range(len(points) - 1):` | `->L190`, `->L195` | **defensive** -- Trunk-row entry bow (#1315). Exercised by tests/fixtures/regressions/entry_trunk_row_bow.mmd, which is kept out of the validate=True corpus and the gate-coverage corpus (it exercises the always-on render path only), so no clean corpus fixture takes this arm. The render-path invariant test test_entry_trunk_row_bow_clears_non_consumer covers the drawn detour. |
| 192 | `for bbox in footprints:` | `->L189`, `->L193` | **defensive** -- Trunk-row entry bow (#1315). Exercised by tests/fixtures/regressions/entry_trunk_row_bow.mmd, which is kept out of the validate=True corpus and the gate-coverage corpus (it exercises the always-on render path only), so no clean corpus fixture takes this arm. The render-path invariant test test_entry_trunk_row_bow_clears_non_consumer covers the drawn detour. |
| 193 | `if segment_intersects_bbox(x1, y1, x2, y2, bbox):` | `->L192`, `->L194` | **defensive** -- Trunk-row entry bow (#1315). Exercised by tests/fixtures/regressions/entry_trunk_row_bow.mmd, which is kept out of the validate=True corpus and the gate-coverage corpus (it exercises the always-on render path only), so no clean corpus fixture takes this arm. The render-path invariant test test_entry_trunk_row_bow_clears_non_consumer covers the drawn detour. |
| 224 | `if not st or st.is_port:` | `->L225` |  |
| 230 | `and line not in graph.station_lines(sid)` | `->L232` | **defensive** -- Trunk-row entry bow (#1315). Exercised by tests/fixtures/regressions/entry_trunk_row_bow.mmd, which is kept out of the validate=True corpus and the gate-coverage corpus (it exercises the always-on render path only), so no clean corpus fixture takes this arm. The render-path invariant test test_entry_trunk_row_bow_clears_non_consumer covers the drawn detour. |
| 233 | `if not blockers:` | `->L236` | **defensive** -- Trunk-row entry bow (#1315). Exercised by tests/fixtures/regressions/entry_trunk_row_bow.mmd, which is kept out of the validate=True corpus and the gate-coverage corpus (it exercises the always-on render path only), so no clean corpus fixture takes this arm. The render-path invariant test test_entry_trunk_row_bow_clears_non_consumer covers the drawn detour. |
| 244 | `if lead_end - sx < min_straight or tx - tail_start < min_straight:` | `->L245`, `->L246` | **defensive** -- Trunk-row entry bow (#1315). No-room return: the flat lead-in before the rise, or the flat tail after the drop, is shorter than a port's minimum straight. A corpus blocker sits far enough from both the port and the target to leave room, so the fall-through to a plain straight run is defensive. |
| 246 | `if over_end - over_start < 2 * ctx.curve_radius:` | `->L247`, `->L249` | **defensive** -- Trunk-row entry bow (#1315). No-room return: the flat run over the blocker(s) is shorter than two corner radii, so the bump's corners would overlap. The blocker-span-plus-clearance always exceeds 2*CURVE_RADIUS for corpus fixtures; defensive. |
| 249 | `for sign in _bow_side_order(section_stations, blockers, sy, over_start, over_end):` | `->L250`, `->L261` | **defensive** -- Trunk-row entry bow (#1315). Loop-exhausted fall-through (both sides fail the clearance check): every corpus bow clears on its first (roomier) side, so the exit past the loop that returns a plain straight run is defensive. |
| 259 | `if _bow_clears_markers(points, section_stations):` | `->L249`, `->L260` | **defensive** -- Trunk-row entry bow (#1315). Retry arm (the roomier side's bow rakes a marker, so the other side is tried): _bow_side_order puts the side with the farthest off-trunk marker first, and the fixed bump height clears it, so no corpus fixture falls to the second side; defensive. |
| 279 | `for st in section_stations:` | `->L280`, `->L293` | **defensive** -- Trunk-row entry bow (#1315). Exercised by tests/fixtures/regressions/entry_trunk_row_bow.mmd, which is kept out of the validate=True corpus and the gate-coverage corpus (it exercises the always-on render path only), so no clean corpus fixture takes this arm. The render-path invariant test test_entry_trunk_row_bow_clears_non_consumer covers the drawn detour. |
| 280 | `if st.id in blocker_ids:` | `->L281`, `->L282` | **defensive** -- Trunk-row entry bow (#1315). Exercised by tests/fixtures/regressions/entry_trunk_row_bow.mmd, which is kept out of the validate=True corpus and the gate-coverage corpus (it exercises the always-on render path only), so no clean corpus fixture takes this arm. The render-path invariant test test_entry_trunk_row_bow_clears_non_consumer covers the drawn detour. |
| 282 | `if not (` | `->L287`, `->L288` | **defensive** -- Trunk-row entry bow (#1315). Exercised by tests/fixtures/regressions/entry_trunk_row_bow.mmd, which is kept out of the validate=True corpus and the gate-coverage corpus (it exercises the always-on render path only), so no clean corpus fixture takes this arm. The render-path invariant test test_entry_trunk_row_bow_clears_non_consumer covers the drawn detour. |
| 289 | `if dy < -COORD_TOLERANCE_FINE:` | `->L290`, `->L291` | **defensive** -- Trunk-row entry bow (#1315). Exercised by tests/fixtures/regressions/entry_trunk_row_bow.mmd, which is kept out of the validate=True corpus and the gate-coverage corpus (it exercises the always-on render path only), so no clean corpus fixture takes this arm. The render-path invariant test test_entry_trunk_row_bow_clears_non_consumer covers the drawn detour. |
| 291 | `elif dy > COORD_TOLERANCE_FINE:` | `->L279`, `->L292` |  |
| 309 | `if not (tgt.x - src.x <= 0 and abs(dy) > CROSS_ROW_THRESHOLD and not same_section):` | `->L311` | **defensive** -- _route_fold_edge: the not-same_section precondition is never satisfied at the intra-section handlers, so the fold (False) arm is never taken: cross-section edges are consumed by _route_inter_section upstream (0/391 corpus fixtures reach this branch with differing sections), and a sectionless flat graph's serpentine fold is routed before this handler too (verified: a flat fold over the threshold does not reach the arm). The cross-row fold branch is shadowed. Reclassified candidate-dead -> defensive (#762): corpus instrumentation across 128 fixtures confirms the un-exercised arm is never taken. |
| 476 | `if abs(tgt.x - src.x) >= COORD_TOLERANCE:` | `->L478` | **defensive** -- _route_near_zero_gap_straight: a same-section, non-TB intra edge with \|tgt.x - src.x\| < COORD_TOLERANCE (1px) and non-zero dy is not constructible, so the straight-return (False) arm is never taken: layering assigns increasing X along edges so the minimum \|dx\| at _route_intra_section across the corpus is 50px (port spacing / off-track output lead). TB same-track edges that could reach \|dx\|=0 are intercepted by _route_tb_internal before this handler. Reclassified candidate-dead -> defensive (#762): corpus instrumentation across 128 fixtures confirms the un-exercised arm is never taken. |
| 547 | `if not (same_sec or is_exit_port):` | `->L548` | **defensive** -- _is_side_branch_ascent reaches this classification (44/391 corpus fixtures) but no fixture takes the reject arm: a target sitting within offset_step*2 of the source section's trunk Y is always a same-section internal station or that section's exit port. The arm needs a target at the source's trunk Y that belongs to a different section or is a non-exit port, which the trunk-alignment geometry does not produce. Reclassified candidate-dead -> defensive (#762): corpus instrumentation across 128 fixtures confirms the un-exercised arm is never taken. |
| 582 | `if not preds or not succs:` | `->L583` | **defensive** -- A bypass V exists to carry lines from one station to another, so by the time _bypass_v_is_one_way_step runs (only for a bypass endpoint of a routed edge) the V always has at least one predecessor and one successor edge. The empty-list return-False arm guards a malformed graph rather than a topology, so it stays un-exercised. |

## `member_geometry.py`

64/86 gates fully exercised.

Gates with an un-exercised arm:

| Line | Gate | Un-exercised arm(s) | Triage |
| ---: | --- | --- | --- |
| 207 | `if route is None:` | `->L208` |  |
| 214 | `if route is None:` | `->L215` |  |
| 233 | `if ctx.exit_turns is not None:` | `->L235` |  |
| 235 | `if ctx.convergences is not None:` | `->L237` |  |
| 347 | `if route.exit_lane_transition_plan_id is not None:` | `->L348` |  |
| 349 | `if route.fan_plan_id is not None or route.fan_route_emitter is not None:` | `->L350` |  |
| 486 | `if coordinate is not None:` | `->L443` |  |
| 558 | `if overlap <= MIN_CORRIDOR_Y_OVERLAP:` | `->L559` |  |
| 726 | `if required <= 0.0:` | `->L727` |  |
| 729 | `if claim.system_id != candidate_item.candidate.system_id and (` | `->L732` |  |
| 733 | `if claim.system_id == candidate_item.candidate.system_id:` | `->L718` |  |
| 871 | `elif abs(start[0] - end[0]) <= COORD_TOLERANCE:` | `->L856` |  |
| 879 | `if lower > upper + COORD_TOLERANCE_FINE:` | `->L880` |  |
| 882 | `if abs(shift) <= COORD_TOLERANCE_FINE:` | `->L884` |  |
| 884 | `for route, rank, coordinate, _band in items:` | `->L876`, `->L885` |  |
| 885 | `if axis == 1:` | `->L886`, `->L888` |  |
| 954 | `if movable_run:` | `->L955` |  |
| 997 | `if edge is None:` | `->L998` |  |
| 1001 | `if family_id is None:` | `->L1002` |  |
| 1023 | `if system_id not in failures:` | `->L1027` |  |
| 1104 | `if tuple(route.route_system_owned_segment_ranks) != plan.owned_segment_ranks:` | `->L1105` |  |
| 1110 | `if actual != (channel.start, channel.end):` | `->L1111` |  |

## `normalize.py`

325/448 gates fully exercised.

Gates with an un-exercised arm:

| Line | Gate | Un-exercised arm(s) | Triage |
| ---: | --- | --- | --- |
| 185 | `if s.bbox_w <= 0:` | `->L186` | **defensive** -- Degenerate-bbox guard in _section_intrudes: a laid-out section always has bbox_w > 0; the skip arm guards a zero/negative-width section the layout never produces. |
| 232 | `if mid - half < gap_left - COORD_TOLERANCE:` | `->L233` |  |
| 234 | `if mid + half > gap_right + COORD_TOLERANCE:` | `->L235` |  |
| 414 | `if abs(overlap) > COORD_TOLERANCE:` | `->L415` | **defensive** -- The symmetric-divergence recognizer rejects opposite vertical legs whose spans overlap instead of meeting at one endpoint. Corpus candidates reaching this helper are same-source branch openings and therefore meet at the shared turn Y; the overlapping arm is a defensive rejection for malformed or independently routed channels. |
| 420 | `if channel.idx == 0:` | `->L421` | **defensive** -- A declared gap channel participating in a same-source divergence follows the branch's horizontal opening and therefore cannot be the route's first segment. The zero-index arm defensively rejects a vertical-first route shape. |
| 425 | `if abs(turn[1] - start[1]) > COORD_TOLERANCE or abs(dx) <= COORD_TOLERANCE:` | `->L426` | **defensive** -- The opening segment before a declared same-source divergence channel is a non-degenerate horizontal run by construction. The non-horizontal or zero-length arm defensively rejects a route shape that cannot provide a shared trunk endpoint. |
| 431 | `if a_trunk is None or b_trunk is None:` | `->L432` | **defensive** -- A symmetric divergence is considered only for declared gap channels produced by a same-source branch opening. Those channels follow a horizontal opening run, so both opening trunks resolve; the missing-trunk arm defensively rejects a different route shape. |
| 477 | `if any(abs(base - bases[0]) > COORD_TOLERANCE for base in bases[1:]):` | `->L478` |  |
| 506 | `if not chans:` | `->L507` |  |
| 536 | `if clearance <= 0:` | `->L537` |  |
| 552 | `if any(` | `->L557` |  |
| 617 | `for k, x, y_lo, y_hi, seg_down in iter_vertical_segments(rp):` | `->L620` |  |
| 699 | `if gap_right <= gap_left:` | `->L700` |  |
| 749 | `if gap_right <= gap_left:` | `->L750` |  |
| 893 | `go is not None` | `->L899` |  |
| 894 | `and gt is not None` | `->L899` |  |
| 931 | `if len(pts) < 2:` | `->L932` |  |
| 947 | `if abs(y2 - y1) > COORD_TOLERANCE or abs(x2 - x1) <= COORD_TOLERANCE:` | `->L948` |  |
| 1016 | `if any(abs(channel.x - ref_x) > COORD_TOLERANCE for channel in planned[1:]):` | `->L1017` |  |
| 1017 | `if validate_planned_axes:` | `->L1018`, `->L1019` |  |
| 1063 | `if channel is None:` | `->L1064` |  |
| 1136 | `if planned and max(planned) - min(planned) > COORD_TOLERANCE:` | `->L1137` |  |
| 1142 | `if ref is not None:` | `->L1143` |  |
| 1176 | `if planned and max(planned) - min(planned) > COORD_TOLERANCE:` | `->L1177` |  |
| 1221 | `for group in _merge_feeder_groups(routes, ctx):` | `->L1222` |  |
| 1227 | `if compatibility_channels:` | `->L1221`, `->L1228` |  |
| 1265 | `if not downs or not ups:` | `->L1267` |  |
| 1268 | `for descent in downs:` | `->L-1234`, `->L1269` |  |
| 1270 | `if col in moved:` | `->L1271`, `->L1272` |  |
| 1273 | `for up in ups:` | `->L1274`, `->L1283` |  |
| 1274 | `if up.route.line_id != descent.route.line_id:` | `->L1275`, `->L1276` | **defensive** -- _clear_merge_trunk_opposite_arm skips an up-arm carrying a different line from the down-trunk, since only a same-line opposite arm folds the line back over itself. The corpus's one fork sends a single line both ways, so the different-line skip arm stays un-exercised. Defensive against a fork whose opposite arms carry distinct lines. |
| 1276 | `if abs(up.x - descent.x) > radius:` | `->L1277`, `->L1278` |  |
| 1279 | `if overlap <= COORD_TOLERANCE:` | `->L1280`, `->L1281` | **defensive** -- _clear_merge_trunk_opposite_arm skips an opposite arm within a curve radius of the down-trunk but not overlapping it in Y, since without a shared Y span there is no fold-back to clear. The corpus's qualifying fork overlaps in Y, so the no-overlap skip arm stays un-exercised. Defensive against a near-column opposite arm that clears the trunk vertically. |
| 1283 | `if target_x is None:` | `->L1284`, `->L1285` |  |
| 1286 | `for ch in downs:` | `->L1268`, `->L1287` |  |
| 1287 | `if abs(ch.x - descent.x) <= COORD_TOLERANCE:` | `->L1286`, `->L1288` | **defensive** -- _clear_merge_trunk_opposite_arm moves every down-leg sharing the folding descent's column and skips those on other columns. The corpus's one folding descent carries all its down-legs on a single shared column, so the other-column skip arm stays un-exercised. Defensive against a fork with down-legs spread over several columns. |
| 1314 | `if k == desc.idx + 1:` | `->L1313` | **defensive** -- In _fanout_traverse_spans: the corpus fan-out routes carry the traverse as their only interior horizontal trunk, so the loop matches on the first trunk and the non-match arm is never taken. Defensive. |
| 1365 | `if lower > upper + COORD_TOLERANCE_FINE:` | `->L1366` |  |
| 1377 | `if any(` | `->L1387` |  |
| 1403 | `or abs(p1[0] - p2[0]) > COORD_TOLERANCE` | `->L1406` |  |
| 1404 | `or abs(p2[1] - p3[1]) > COORD_TOLERANCE` | `->L1406` |  |
| 1423 | `if source_col is None:` | `->L1424` |  |
| 1439 | `if route_system_owns_segment_boundary(route, opening.idx):` | `->L1440` |  |
| 1441 | `if (` | `->L1450` |  |
| 1465 | `if new_x <= max(sibling_xs) + ctx.curve_radius + COORD_TOLERANCE:` | `->L1466` |  |
| 1468 | `if _section_intrudes(` | `->L1475` |  |
| 1495 | `if any(` | `->L1499` |  |
| 1513 | `if len(turns) != 1:` | `->L1514` | **defensive** -- In _bundle_divergent_distinct_traverses: every fan in the corpus turns its traverses the same way, so the mixed-direction skip is never taken. Defensive. |
| 1534 | `if any(` | `->L1538` | **defensive** -- Compatibility convergence may not move a planner-owned final channel or reseat a channel through a foreign section. Complete direct-port opposing tails are unplanned and placement reserves their target band; the true arm therefore defends broken ownership or obstruction state. test_planner_owned_opposing_group_is_left_atomic and test_obstructed_opposing_group_is_left_atomic exercise both rejection reasons. |
| 1576 | `elif ctx.merge.entry_port_for.get(rp.edge.source) == rp.edge.target:` | `->L1577` |  |
| 1587 | `for merge_id, hop in hop_by_merge.items():` | `->L1588` |  |
| 1589 | `if not feeders or any(ends_at(route, hop.points[0]) for route in feeders):` | `->L1590`, `->L1591` |  |
| 1594 | `if carrier is None:` | `->L1595`, `->L1596` | **defensive** -- A removable merge-entry hop has feeders, none ending at the hop start, and one ending at the hop destination after feeder normalization. A missing carrier therefore signals broken settled geometry; the guard keeps the hop instead of discarding a visible route. |
| 1603 | `if not covered:` | `->L1605` |  |
| 1659 | `for _ in range(max(1, len(buckets))):` | `->L-1609` | **defensive** -- Bounded fixed-point repair for coincident corner radii. Production routes settle without a second correction pass; the multi-bucket propagation and loop-exhaustion paths are exercised directly by test_unify_refreshes_a_shared_route_after_changing_one_corner. |
| 1677 | `if actual - common <= COORD_TOLERANCE_FINE:` | `->L1679` | **defensive** -- Production coincident turns have enough shared runway after planning and already resolve to the same radius. The correction arm is a defensive repair for directly supplied or future route geometry and is exercised by the coincident-corner unit tests. |
| 1682 | `for _step in range(64):` | `->L1683`, `->L1690` | **defensive** -- Fixed-iteration bisection used only when a defensive coincident-radius repair must lower one route. The correction and completed bisection are exercised by test_unify_uses_widest_radius_all_shared_legs_can_resolve. |
| 1686 | `if resolved < common:` | `->L1687`, `->L1689` | **defensive** -- Both bisection directions belong to the defensive coincident-radius repair rather than ordinary topology dispatch. The short-runway unit case exercises the search and verifies the final common effective radius. |
| 1693 | `if not changed:` | `->L1659` | **defensive** -- The production corpus reaches the immediate fixed point because coincident members already share an effective radius. The continue-for-propagation arm is a defensive repair path covered by test_unify_refreshes_a_shared_route_after_changing_one_corner. |
| 1728 | `if _planner_owns_channel(ch):` | `->L1729` |  |
| 1792 | `if opposing_bundle is not None and {` | `->L1795` | **defensive** -- iter_opposing_entry_confluences requires the complete direct-port line set, while by_port can only add a member through convergence-junction aliasing. Resolved convergence junctions replace the direct approach rather than coexist with a complete direct-port bundle, so unequal ownership sets require an inconsistent route/context pair. test_opposing_group_with_an_extra_mapped_approach_uses_generic_ownership exercises the defensive fallback. |
| 1802 | `if any(` | `->L1811` |  |
| 1883 | `if any(` | `->L1888` |  |
| 1924 | `if has_planned_channel and any(` | `->L1929` |  |
| 1980 | `if any(` | `->L1984` |  |
| 1985 | `if not crossing:` | `->L1992` |  |
| 1995 | `if abs(delta) <= COORD_TOLERANCE:` | `->L1996`, `->L2000` | **defensive** -- _snap_merge_feeder_group skips a merge feeder whose opening descent already sits on the trunk's shared column (no shift needed). The corpus's merge feeders all reach the trunk from a distinct column, so the already-coincident skip arm stays un-exercised. Defensive and idempotent across the routing replays a render performs. |
| 2000 | `for r, k, _y in crossing:` | `->L1957`, `->L2001` |  |
| 2122 | `if rp.curve_radii is None:` | `->L2123` | **defensive** -- curve_radii is always a concrete list for inter-section routes (every inter-section handler sets it); the None arm in _reseat_concentric_flanking is a belt-and-suspenders guard. |
| 2320 | `if route.curve_radii is None:` | `->L2321` | **defensive** -- Fan-opening channels are selected from a real horizontal-to-vertical turn, and production turn builders attach radius metadata to that shape. The None arm protects a synthetic or incomplete RoutedPath. |
| 2392 | `if port is not None and port.is_entry:` | `->L2394` |  |
| 2408 | `if (` | `->L2415` |  |
| 2539 | `if not trunks:` | `->L2540`, `->L2541` | **defensive** -- Shape guard in _merge_convergence_run: a trunk routed as a U-shaped bypass always has an interior horizontal leg for iter_horizontal_trunks to find, so the corpus never takes the None arm. Not provably unreachable across every branch of _route_bypass, so it stays a guard rather than an assert (#1597). |
| 2566 | `if not verticals:` | `->L2567` | **defensive** -- Shape guard in _land_feeder_on_run: a merge branch feeder is built as a descent into a tail, so it always has a vertical leg. Reachable in principle only if a feeder's source Y coincides with the trunk channel level, collapsing the descent below COORD_TOLERANCE (#1597). |
| 2572 | `if k + 2 != len(pts) - 1 or y_hi - y_lo < radius:` | `->L2573` | **defensive** -- Shape guard in _land_feeder_on_run. _route_merge_branch emits exactly four waypoints, but _round_junction_perp_peeloff prepends one for any route whose source doubles as a fan-out junction, which a merge feeder's junction source can be; and the second half rejects a descent too short to re-form its corner. Neither shape occurs in the corpus (#1597). |
| 2584 | `if rp.curve_radii is not None:` | `->L2586` | **defensive** -- Truncating the tail in _land_feeder_on_run must keep curve_radii aligned with the shortened waypoint list. route_along always populates curve_radii, but _round_junction_perp_peeloff clears it to None for the fan-out-junction routes described above, so the field cannot be treated as always present (#1597). |
| 2612 | `if len(rp.points) != 2:` | `->L2613`, `->L2614` |  |
| 2615 | `if abs(lane - _get_offset(ctx, rp.edge.target, rp.line_id)) <= COORD_TOLERANCE:` | `->L2616`, `->L2617` |  |
| 2622 | `if not (riser_spans_lane and riser_ahead):` | `->L2623`, `->L2624` | **defensive** -- Reachability guard in _land_lane_changing_feeder_on_trunk_riser: the trunk's riser has to cross the feeder's lane and stand in the feeder's direction of travel before the feeder can terminate on it. Only one corpus fixture reaches the guard at all (exit_run_three_drop_columns, the sole adjacent merge feeder whose direct run changes lane), and there the riser both spans the lane and stands ahead, so the reject arm needs a trunk that turns before the feeder junction or lands short of its lane. |
| 2646 | `if trunk_rp.convergence_plan_id is not None:` | `->L2648` |  |
| 2649 | `if run is None:` | `->L2650`, `->L2651` | **defensive** -- Consumer of the _merge_convergence_run None arm above; unreachable for exactly the same reason and kept for the same reason (#1597). |
| 2651 | `for rp in others:` | `->L2645`, `->L2652` |  |
| 2652 | `if (rp.edge.source, rp.edge.target, rp.line_id) in merge.branch_edges:` | `->L2653`, `->L2655` |  |
| 2871 | `elif far_y < t.y - COORD_TOLERANCE:` | `->L2865` | **defensive** -- Degenerate-leg guard in _trunk_slot_features: a trunk's flanking legs are genuine vertical legs (far_y differs from the trunk y), so neither the below nor the above classification is skipped; the fall-through arm needs a zero-height leg the layout never produces. |
| 2901 | `if feats is None:` | `->L2902` | **defensive** -- Unused default-arg branch: all three callers of _band_order_crossings pass feats explicitly, so the feats-is-None compute-it arm is never taken. |
| 3071 | `if len(trunks) < 2:` | `->L3072`, `->L3073` | **defensive** -- Early-return in _suboptimal_trunk_bands, called only from the _guard_* validator (phases/guards.py) and a unit test, never on the production render path. The coverage sweep renders with validate=False, so neither arm registers. Defensive (exercised via the validate=True guard / test surface; cf. #728 validate-only precedent). |
| 3075 | `for grp in groups:` | `->L3076`, `->L3094` | **defensive** -- _suboptimal_trunk_bands diagnostic loop: validate-only, not reached by the render-path coverage sweep (see the len(trunks)<2 note). |
| 3076 | `if len({id(t.route) for t in grp}) < 2:` | `->L3077`, `->L3078` | **defensive** -- Single-route-group skip in _suboptimal_trunk_bands, validate-only: called from the _guard_* validator and a unit test, not the production render path the validate=False sweep exercises. Defensive. |
| 3078 | `if not any(not t.route.normalize_exempt for t in grp):` | `->L3079`, `->L3080` | **defensive** -- _suboptimal_trunk_bands diagnostic guard: validate-only, not on the render path. |
| 3080 | `for sign in (1, -1):` | `->L3075`, `->L3081` | **defensive** -- _suboptimal_trunk_bands direction loop: validate-only, not on the render path. |
| 3083 | `if len(slots) < 2 or len(slots) > _MAX_BAND_PERMUTE:` | `->L3084`, `->L3085` | **defensive** -- _suboptimal_trunk_bands slot-count guard: validate-only, not on the render path. |
| 3092 | `if best < cur:` | `->L3080`, `->L3093` | **defensive** -- _suboptimal_trunk_bands crossing-comparison: validate-only, not on the render path. |
| 3242 | `if band is not None:` | `->L3248` | **defensive** -- Same-line dogleg: the band-found arm is exercised by dogleg_exempt_sameline; the band-is-None arm is defensive (both colliding trunks route through _center_inter_row_channel, so the trunk Y always resolves to a gap band). |
| 3263 | `if down_ok and up_ok and (cross_down is None) != (cross_up is None):` | `->L3265` | **defensive** -- Same-line dogleg side selection. The crossing-decided true arm (->1297, pick the side whose riser does not pierce the exempt run) is exercised by dogleg_exempt_sameline. The false fall-through (->1298) needs either an out-of-band side or a crossing-equal tie, neither reachable by a clean topology: the reachable dogleg regime rigidly fixes the inter-row band so down_room/up_room stay >= min_sep=6 (observed 14/26 across the corpus and every constructed variant; perturbing the grid loses the regime), and the only realizable tie is both-cross (a crossing-free tie needs degenerate zero-length exempt risers) which co-occurs with section-crossing / dogleg-crosses-exempt guard failures and is never a shippable render. |
| 3265 | `elif down_ok and (not up_ok or prefer_down):` | `->L3266`, `->L3267` | **defensive** -- Same-line dogleg tie / out-of-band fallback, reached only after the L1296 if is false, which a clean topology never produces (see that gate): the true arm (->1299, lean down on a tie) needs a crossing-free tie that requires degenerate risers, and the false arm (->1300) needs down_ok False, which the regime's fixed inter-row band (down_room >= min_sep) never yields. |
| 3267 | `elif up_ok:` | `->L3268`, `->L3270` | **defensive** -- Same-line dogleg move-up / give-up fallback, reached only when both L1296 and L1298 are false. The true arm (->1301, move up because the lower gap is too tight) needs down_ok False and the else arm (->1303, leave fused) needs both rooms below min_sep; the reachable regime's inter-row band is auto-widened so both rooms stay >= min_sep=6 (observed 14/26), so neither is reached by a valid topology. |
| 3293 | `if band is not None:` | `->L3298` | **defensive** -- Distinct-line dogleg: the band-found arm (->1327) is exercised by dogleg_exempt_distinct (#702). The remaining band-is-None arm (->1331) is defensive: a trunk reaching this second loop collides with an exempt bypass obstacle that lives in an inter-row gap, so _inter_row_gap_band(ctx, t.y) always resolves to a band - the same mechanism already accepted for the sibling 'if band is not None:::#1'. |
| 3308 | `if below_ok and above_ok and (cross_below is None) != (cross_above is None):` | `->L3310` | **defensive** -- Distinct-line dogleg side selection. The crossing-decided true arm (->1342) is exercised by dogleg_exempt_distinct. The false fall-through (->1343) needs a crossing-equal tie; a crossing-free tie requires degenerate zero-length exempt risers and the only realizable tie is both-cross, which trips _guard_no_dogleg_crosses_exempt_trunk plus the section-crossing guards and is never a shippable render. |
| 3310 | `elif below_ok and (not above_ok or prefer_below):` | `->L3311`, `->L3312` | **defensive** -- Distinct-line dogleg tie fallback, reached only after L1341 is false (a tie), which a clean topology never produces (see that gate). The true arm (->1344, lean below) would fire on such a tie; the false arm (->1345) is unreachable because below_ok always holds and prefer_below is always true (t.y == hit.y in this regime). |
| 3312 | `elif above_ok:` | `->L3313`, `->L3315` | **defensive** -- Distinct-line dogleg move-up/skip branch: unreachable because the below branch is always taken (t.y == hit.y, below_ok true); covers both the move-up and the gap-too-tight continue. |
| 3330 | `held[0] - COORD_TOLERANCE < coord < held[1] + COORD_TOLERANCE` | `->L3331` | **defensive** -- Held-axis bracket in _run_enters_section, the section-intrusion veto's first test. No corpus separation target lands within a section's extent on the held axis, so the inside arm never fires; the same defensive reasoning as the veto it serves. |
| 3331 | `and span[0] < crossed[1]` | `->L3323`, `->L3332` | **defensive** -- Crossed-axis overlap test in _run_enters_section, reached only for a target already bracketed on the held axis. No corpus separation target is, so this operand's short-circuit arm never fires. |
| 3332 | `and crossed[0] < span[1]` | `->L3323`, `->L3334` | **defensive** -- Second half of the crossed-axis overlap test in _run_enters_section, reached only once the first half holds. No corpus separation target reaches it, so its short-circuit arm never fires. |
| 3401 | `if i in relocated:` | `->L3402` | **defensive** -- Re-entry skip in the co-travelling separation worklist. A track is re-queued only while it is still un-relocated, so the second copy of a track queued twice before its first pop is the only way to reach the skip; across the corpus every re-queued track is popped before it moves, so the taken arm never fires. Defensive against a crowded stack whose neighbours re-queue one track twice, where taking it twice would break the one-move-per-track bound. |
| 3412 | `if any(` | `->L3416` |  |
| 3458 | `if sg[0].sign_x != t.sign_x:` | `->L3459` | **defensive** -- Both callers of _coincident_trunk_slots pre-filter input to a single traversal direction (band = [t for t in grp if t.sign_x == sign]), so within the function sg[0].sign_x != t.sign_x is always False; the opposing-direction continue is unreachable. Reclassified candidate-dead -> defensive (#762): corpus instrumentation across 128 fixtures confirms the un-exercised arm is never taken. |
| 3489 | `if rp.curve_radii is None:` | `->L3490` | **defensive** -- curve_radii is always set for inter-section routes; the None arm in _restack_htrunk is a defensive guard. |
| 3501 | `if 0 <= k - 1 < len(rp.curve_radii):` | `->L3503` | **defensive** -- Bounds guard in _restack_htrunk: trunk index k >= 1 so k-1 >= 0 is always in range; the skip arm is defensive. |
| 3503 | `if k < len(rp.curve_radii) and k + 2 < len(pts):` | `->L-3468` | **defensive** -- Bounds guard in _restack_htrunk: for a real interior trunk k and k+2 are in range; the skip arm is defensive. |
| 3650 | `if len(targets) != 1:` | `->L3651` |  |
| 3660 | `if col is None:` | `->L3661` | **defensive** -- Defensive guard in _convergence_line_order: a peel-off riser channel always resolves to a source-section column, so the unresolved-column early return is never taken on the corpus. |
| 3701 | `if not 0 <= far < len(pts):` | `->L3702` |  |
| 3704 | `if abs(dx) > COORD_TOLERANCE:` | `->L3698` |  |
| 3706 | `if not votes:` | `->L3707` |  |
| 3810 | `if ca != cb:` | `->L3812` |  |
| 3812 | `if rep_x[a] != rep_x[b]:` | `->L3813`, `->L3814` | **defensive** -- The routing system assigns distinct x offsets to every line sharing a column gap, so two distinct lines in one gap-bundle corridor never share a representative x; the equal-rep_x tiebreak fall-through in _distinct_line_order is unreachable (exhaustive corpus scan confirms). Reclassified candidate-dead -> defensive (#762): corpus instrumentation across 128 fixtures confirms the un-exercised arm is never taken. |
| 3842 | `if rp.curve_radii is None:` | `->L3843` | **defensive** -- curve_radii is always set for inter-section routes; the None arm in _restack_channel is a defensive guard. |
| 3845 | `if not 0 <= radius_idx < len(rp.curve_radii):` | `->L3846` |  |
| 3868 | `if abs(ly - pts[1][1]) < COORD_TOLERANCE:` | `->L-3819` | **defensive** -- A re-stacked gap channel at points-index 1 always has a horizontal lead-in (L-shape and bypass routes open with a horizontal segment); routes opening non-horizontally are normalize_exempt and skipped by the materialization. The non-horizontal-lead return in _restack_channel is unreachable. Reclassified candidate-dead -> defensive (#762): corpus instrumentation across 128 fixtures confirms the un-exercised arm is never taken. |
| 3898 | `if anchor_section_id is not None and anchor_side is not None:` | `->L3900` | **defensive** -- _gap_channel_base falls back to the column gap when passed a null anchor. All six call sites (in _route_bypass) pass the bypass source/target section id, which is None only when that endpoint has no resolvable section (src_sec_id/tgt_sec_id in _route_bypass); the corpus has no bypass with a sectionless endpoint, so the null-anchor arm is never taken. Defensive guard for a sectionless bypass endpoint. |
| 3953 | `elif right_ok:` | `->L3956` |  |
| 4002 | `if s.bbox_w <= 0:` | `->L4003` | **defensive** -- Degenerate-bbox guard in _v_segment_crosses_other_section: mirror of the _h_segment_penetrates_section guard; sections always have positive width. |
| 4374 | `if not blockers:` | `->L4375` |  |

## `offsets.py`

412/518 gates fully exercised.

Gates with an un-exercised arm:

| Line | Gate | Un-exercised arm(s) | Triage |
| ---: | --- | --- | --- |
| 142 | `if edge.target in inbound:` | `->L144` | **defensive** -- inbound is pre-built as {sid: set() for sid in graph.stations}, so every station id in the graph is a key. All edge endpoints are guaranteed to be in graph.stations after parsing (regular nodes via register_station, ports via add_port->add_station, junctions via add_station), so edge.target is always found in inbound and the False branch (54->56) is never taken. |
| 144 | `if edge.source in outbound:` | `->L141` | **defensive** -- outbound is pre-built from graph.stations keys identically to inbound. All edge sources are registered stations by construction (regular nodes, ports, junctions), so edge.source is always found in outbound and the False branch (56->53) is never taken. |
| 406 | `if ordered != expected:` | `->L407` | **defensive** -- The raise arm of _assert_sections_anchored_on_trunk, a backstop on the postcondition of _reindex_section_local: an independent section (no flat-frame neighbour) always lands on the contiguous top-anchored levels 0,step,...,(m-1)*step by construction, so the mismatch arm never fires across the corpus. It guards against a future change that stops re-anchoring such a section. |
| 449 | `while cur is not None and cur not in seen:` | `->L458` |  |
| 484 | `if slot is None:` | `->L485` |  |
| 497 | `if neighbour is None:` | `->L498` |  |
| 621 | `if reverse:` | `->L622` | **needs-review** -- Reachable only on a reconvergence section that is also a serpentine fold, where the fold corner crosses lines mid-curve and the return-row bundle sits one offset too low (#705). Exercise with a clean fixture once #705 is fixed. |
| 652 | `if section is None:` | `->L653` |  |
| 676 | `if not all(lid in feeder_off for lid in continuing):` | `->L677` |  |
| 684 | `if lid in new_off:` | `->L683` |  |
| 706 | `if section is not None:` | `->L704` |  |
| 708 | `if len(set(feeder_row.values())) < 2:` | `->L709` |  |
| 726 | `for pid in section.entry_ports:` | `->L736` |  |
| 728 | `if entry_port is None:` | `->L729` |  |
| 730 | `for edge in graph.edges_to(pid):` | `->L726` |  |
| 771 | `if seam is None:` | `->L772` |  |
| 793 | `if not all(lid in feeder_off for lid in primary_lines):` | `->L794` |  |
| 842 | `if new_order is None:` | `->L845` |  |
| 923 | `if cur in seen:` | `->L924` | **needs-review** -- In the exit-only-line reorder / same-Y offset propagation; reachable only via a multi-line LR/RL section with a perpendicular (TOP/BOTTOM) exit port, which the engine lays out as a station-as-elbow + collinear overlay PhaseInvariantError (#706). Exercise with a clean fixture once #706 is fixed. |
| 933 | `if not src.is_port and src.section_id == sec_id:` | `->L926` |  |
| 1019 | `if not target_id:` | `->L1020` | **defensive** -- An exit-only line at station sid is identified by being present in station_lines(sid) but absent from ctx.inbound[sid]. station_lines() returns lines appearing in any edge whose source or target is sid. For a line to be exit-only at sid, it must appear in at least one outbound edge from sid; otherwise it would not appear in station_lines(sid) at all. If an outbound edge exists, outbound_target[(sid, lid)] is always set, so target_id is never None when execution reaches L422. Reclassified candidate-dead -> defensive (#762): corpus instrumentation across 128 fixtures confirms the un-exercised arm is never taken. |
| 1022 | `if not target_st:` | `->L1023` | **defensive** -- After parse and layout, every edge target ID in a MetroGraph maps to a station in graph.stations. The guard protects against a structurally malformed graph where an edge references a nonexistent station ID, which cannot occur with valid .mmd input processed through the parser and _resolve_sections(). |
| 1039 | `for other in lines:` | `->L1047` | **defensive** -- In _reorder_one_exit_line, the loop over `lines` always finds the swap partner: desired_off is the min or max of all_offs, which by construction is held by another line, so the loop-exhausted-without-match arm is never taken (0/128). Defensive. Reclassified defensive (#762). |
| 1047 | `if swap_lid is None:` | `->L1048` | **defensive** -- In _reorder_one_exit_line, swap_lid is None only if the loop over `lines` exhausts without a match, but desired_off (min or max of all_offs) is always held by another line, so the early-return guard never fires (0/128). Defensive. Reclassified defensive (#762). |
| 1092 | `if (cur_sid, cur_lid) in visited:` | `->L1093` | **needs-review** -- In the exit-only-line reorder / same-Y offset propagation; reachable only via a multi-line LR/RL section with a perpendicular (TOP/BOTTOM) exit port, which the engine lays out as a station-as-elbow + collinear overlay PhaseInvariantError (#706). Exercise with a clean fixture once #706 is fixed. |
| 1104 | `if abs(nbr_cur - new_off) < _OFFSET_EQ_TOLERANCE:` | `->L1105` | **needs-review** -- In the exit-only-line reorder / same-Y offset propagation; reachable only via a multi-line LR/RL section with a perpendicular (TOP/BOTTOM) exit port, which the engine lays out as a station-as-elbow + collinear overlay PhaseInvariantError (#706). Exercise with a clean fixture once #706 is fixed. |
| 1111 | `if len(nbr_lines) < 2:` | `->L1115` | **needs-review** -- In the exit-only-line reorder / same-Y offset propagation; reachable only via a multi-line LR/RL section with a perpendicular (TOP/BOTTOM) exit port, which the engine lays out as a station-as-elbow + collinear overlay PhaseInvariantError (#706). Exercise with a clean fixture once #706 is fixed. |
| 1115 | `for other_lid in nbr_lines:` | `->L1097`, `->L1116` | **needs-review** -- In the exit-only-line reorder / same-Y offset propagation; reachable only via a multi-line LR/RL section with a perpendicular (TOP/BOTTOM) exit port, which the engine lays out as a station-as-elbow + collinear overlay PhaseInvariantError (#706). Exercise with a clean fixture once #706 is fixed. |
| 1116 | `if other_lid == cur_lid:` | `->L1117`, `->L1118` | **defensive** -- Inside _propagate_offset_swap's same-Y collision-swap block: across the corpus no same-Y adjacent multi-line neighbour needs an offset swap, so the loop body (incl. this self-skip guard) never executes (0/128). Defensive guard for a same-Y multi-line offset collision absent from current topologies. Reclassified defensive (#762). |
| 1118 | `if (` | `->L1115`, `->L1122` | **defensive** -- Inside _propagate_offset_swap's same-Y collision-swap block: across the corpus no same-Y adjacent multi-line neighbour needs an offset swap, so the inner collision check never executes (0/128). Defensive guard for a same-Y multi-line offset collision absent from current topologies. Reclassified defensive (#762). |
| 1126 | `if max_steps <= 0:` | `->L1127` | **defensive** -- max_steps = len(graph.stations) * len(graph.lines). Each BFS iteration dequeues one (station, line) pair and marks it visited; visited pairs are skipped on re-encounter. The number of distinct (station, line) pairs is bounded by \|stations\| * \|lines\|, so the BFS can perform at most that many productive steps before the queue empties. The `while queue and max_steps > 0` condition therefore always exits via the queue-empty branch; max_steps reaches zero only if more productive steps occur than unique pairs, which is impossible in a finite graph. |
| 1154 | `if lid not in seen:` | `->L1151` | **defensive** -- The iterator at L554 is `sorted(set(sec_entry_lines), ...)`. The outer `set()` deduplicates the list before iteration, so each `lid` appears exactly once and `seen` never contains it when processed. The false arm (lid already in seen) is logically unreachable — the dedup happens at set construction, making the `seen` guard dead code. Reclassified candidate-dead -> defensive (#762): corpus instrumentation across 128 fixtures confirms the un-exercised arm is never taken. |
| 1281 | `for _ in range(len(graph.stations) + 1):` | `->L1298` |  |
| 1285 | `if edge is None:` | `->L1286` |  |
| 1288 | `if nxt in seen:` | `->L1289` |  |
| 1372 | `if not src_st.is_port:` | `->L1370` |  |
| 1376 | `if internal_offs:` | `->L1364` | **defensive** -- Exit ports are only created when inter-section edges exist (resolve.py `_create_port_stations` iterates `exit_group_edges` which is populated only from actual edges). Every TB LEFT/RIGHT exit port therefore has at least one edge from an internal station, so `internal_offs` is never empty when the loop is entered. The false arm is a defensive guard against an impossible empty-feeder state. |
| 1406 | `if not src_st.is_port:` | `->L1404` |  |
| 1537 | `if anchor_feeders:` | `->L1540` |  |
| 1559 | `if port_off is not None:` | `->L1557` | **defensive** -- _compute_base_offsets runs before junction propagation and creates an offset for every station-line pair. A divergence exit port and its junction carry the same connector lines, so this lookup always succeeds; the missing-offset arm protects an incomplete synthetic context. |
| 1586 | `if port_st is None:` | `->L1587` |  |
| 1661 | `if not src.is_port:` | `->L1662` |  |
| 1818 | `if not src.is_port:` | `->L1819` |  |
| 1845 | `if paired_off is not None:` | `->L1843` | **defensive** -- _compute_base_offsets sets ctx.offsets[(sid, lid)] for every station-line pair before _propagate_lr_rl_exit_to_entry runs. Exit ports are stations. The for-loop at L791 iterates graph.station_lines(port_id) which equals graph.station_lines(exit_port_id) (enforced by the exit_lines != entry_lines guard at L788). For each lid, ctx.offsets.get((exit_port_id, lid)) is therefore never None. The loop-back arc (793,791) for the paired_off-is-None case is structurally unreachable. |
| 1851 | `if not tgt_st.is_port:` | `->L1849` |  |
| 1899 | `if set(inherited) != set(graph.station_lines(port_id)):` | `->L1900` | **defensive** -- _inherit_level_convergence_entry_offsets skips a convergence entry port when the lines it inherits from its immediate feeders do not cover every line the port carries. A convergence entry's lines are, by construction, exactly the union of its inbound edges' lines, and each inbound edge supplies that line's feeder offset, so the inherited set always equals the port's line set and the mismatch arm is never taken. Defensive against a port line with no inbound edge (a malformed graph). |
| 1936 | `if entry.side not in (PortSide.LEFT, PortSide.RIGHT):` | `->L1937` | **defensive** -- In _align_flat_tb_exit_to_entry: scopes the flat-seam exit/entry reconciliation to horizontal (LEFT/RIGHT) entry ports, whose per-line offsets are Y like the TB exit's. A TOP/BOTTOM entry stores X-offsets, so copying across would be a category error. No corpus fixture routes a TB LEFT/RIGHT exit directly into a TOP/BOTTOM entry at the same Y, so the skip arm is never taken; defensive against that cross-axis seam. |
| 1944 | `if len(shared) < 2:` | `->L1945` | **defensive** -- In _align_flat_tb_exit_to_entry: scopes the flat-seam reconciliation to multi-line bundles, where a constant per-line offset delta means the lines share an order and only the bundle anchoring differs (the auto-folded serpentine case in #1198). A single shared line carries no bundle order to preserve and is left to the single-line corridor passes; no corpus fixture reaches the pass with fewer than two shared lines, so the skip arm is never taken. |
| 2033 | `if edge.line_id not in lines:` | `->L2034` |  |
| 2090 | `for edge in graph.edges_from(port_id):` | `->L2099` |  |
| 2094 | `if tgt.is_port:` | `->L2095` |  |
| 2099 | `if target_id is None:` | `->L2100` |  |
| 2184 | `if not sec_stations:` | `->L2185` | **defensive** -- The parser calls _remove_empty_sections() (resolve.py:26) which deletes any section whose station_ids list is empty before port creation. Ports are only added to station_ids during _create_port_stations() (resolve.py:527), which runs AFTER _remove_empty_sections(). Therefore every section in graph.sections at layout time has at least one non-port station; sec_stations is never empty. |
| 2229 | `if station.section_id is not None:` | `->L2221` |  |
| 2238 | `if port.section_id is None:` | `->L2239` |  |
| 2251 | `if section is None:` | `->L2252` |  |
| 2289 | `if tgt_port is None or not tgt_port.is_entry:` | `->L2290` |  |
| 2291 | `if tgt_port.side not in (PortSide.LEFT, PortSide.RIGHT):` | `->L2292` |  |
| 2293 | `if tgt_port.section_id in ctx.tb_sections:` | `->L2294` |  |
| 2298 | `if not _stores_reflected(ctx, tgt_port.section_id) and reversed_seam:` | `->L2299` |  |
| 2301 | `if off is not None:` | `->L2287` |  |
| 2321 | `if not any(` | `->L2324` | **defensive** -- The L920 check returns None when offsets are EXACTLY equal to expected (exact float ==). The L926 check returns None when differences are less than _OFFSET_EQ_TOLERANCE (0.001). Since all offsets are integer multiples of OFFSET_STEP (3.0px), any non-zero difference is exactly 3.0 or more, which is >> 0.001. So L920 catches exact matches and L926 can never catch a case L920 missed; the near-equal window (0,0.001) is unreachable with 3.0px discrete steps. |
| 2339 | `if len(seed_lines) < 2:` | `->L2340` |  |
| 2345 | `if not fixed_here or not movable:` | `->L2346` |  |
| 2364 | `for lid in above:` | `->L2365` |  |
| 2368 | `if not any(` | `->L2371` |  |
| 2446 | `for _ in range(len(seed_lines)):` | `->L2487` |  |
| 2460 | `if not changed_lids:` | `->L2461` |  |
| 2479 | `if pending is None:` | `->L2480` |  |
| 2608 | `if max_steps <= 0:` | `->L2609` | **defensive** -- max_steps = n_sec_stations * len(graph.lines). The BFS visits each (station_id, line_id) pair at most once (the visited set at L1017 prevents revisits). The total distinct pairs is bounded by n_sec_stations * len(graph.lines) = max_steps. Since max_steps decrements by 1 per loop iteration and the loop exits when the queue is empty (naturally after at most max_steps unique visits), the condition max_steps <= 0 is never True; the while-loop guard already prevents the body from executing when the queue is empty. |
| 2622 | `if sec_a is None and id_a in graph.ports:` | `->L2623` | **defensive** -- The true arm requires sec_a is None AND id_a in graph.ports simultaneously. Junction stations (section_id=None) are added only via graph.add_junction() which calls graph.add_station() but NOT graph.add_port(). graph.add_port() (model.py:483) requires a Port object with a non-None section_id (Port.section_id is typed str, not Optional[str], per model.py:218). Therefore no station with section_id=None can ever be in graph.ports; the conjunction sec_a is None AND id_a in graph.ports is unsatisfiable. Reclassified candidate-dead -> defensive (#762): corpus instrumentation across 128 fixtures confirms the un-exercised arm is never taken. |
| 2624 | `if sec_b is None and id_b in graph.ports:` | `->L2625` | **defensive** -- Same argument as L1084: id_b in graph.ports requires the station to have been registered via graph.add_port(), which always supplies a non-None section_id for the created station. A station with section_id=None (sec_b is None) is a junction created by add_junction(), which never calls add_port(). The precondition sec_b is None AND id_b in graph.ports is therefore unsatisfiable. Reclassified candidate-dead -> defensive (#762): corpus instrumentation across 128 fixtures confirms the un-exercised arm is never taken. |
| 2687 | `if (tgt_id, lid) not in ctx.offsets:` | `->L2688` | **defensive** -- The True branch (missing entry-port offset -> ok=False) is unreachable because phase 7 (_compute_entry_port_offsets) runs before this function (phase 8a) and assigns an offset for every (entry_port_id, line_id) pair in the graph. By the time _align_junction_to_entry_port runs, ctx.offsets is populated for all entry ports, so the guard never fires. Reclassified candidate-dead -> defensive (#762): corpus instrumentation across 128 fixtures confirms the un-exercised arm is never taken. |
| 2698 | `if all(` | `->L2703` | **defensive** -- Reaches the realignment body of _align_junction_to_entry_port only via a TB-reversed two-line bundle fanning into a junction (the #704 boundary fix is in, so this body is now provably reachable). The junction_entry_reversed_fold fixture now ships that topology; with the bundle order carried concentrically through the reversal corners the junction offsets already match their entry ports, so the realignment body short-circuits before its offset-writing arms and they stay un-exercised corpus-wide. Defensive. The already-aligned arm (->1190) is exercised by the corpus; the body arm (->1192) is the reachable-but-defective one. |
| 2705 | `for edge in graph.edges_to(jid):` | `->L2706`, `->L2717` | **defensive** -- Reaches the realignment body of _align_junction_to_entry_port only via a TB-reversed two-line bundle fanning into a junction (the #704 boundary fix is in, so this body is now provably reachable). The junction_entry_reversed_fold fixture now ships that topology; with the bundle order carried concentrically through the reversal corners the junction offsets already match their entry ports, so the realignment body short-circuits before its offset-writing arms and they stay un-exercised corpus-wide. Defensive. |
| 2707 | `if src_port and not src_port.is_entry:` | `->L2708`, `->L2714` | **defensive** -- Reaches the realignment body of _align_junction_to_entry_port only via a TB-reversed two-line bundle fanning into a junction (the #704 boundary fix is in, so this body is now provably reachable). The junction_entry_reversed_fold fixture now ships that topology; with the bundle order carried concentrically through the reversal corners the junction offsets already match their entry ports, so the realignment body short-circuits before its offset-writing arms and they stay un-exercised corpus-wide. Defensive. Its else arm (->1203) is additionally defensive: the sole feeder of a >=2-line fan-out junction is its exit port, a non-entry port. |
| 2708 | `if feeding_exit is None:` | `->L2709`, `->L2710` | **defensive** -- Reaches the realignment body of _align_junction_to_entry_port only via a TB-reversed two-line bundle fanning into a junction (the #704 boundary fix is in, so this body is now provably reachable). The junction_entry_reversed_fold fixture now ships that topology; with the bundle order carried concentrically through the reversal corners the junction offsets already match their entry ports, so the realignment body short-circuits before its offset-writing arms and they stay un-exercised corpus-wide. Defensive. |
| 2710 | `elif feeding_exit != edge.source:` | `->L2705`, `->L2711` | **defensive** -- This arm (single_exit=False due to multiple distinct exit ports feeding one junction) requires one junction to receive edges from two different exit ports. But _rewrite_edges_with_junctions in resolve.py creates exactly one junction per exit port fan-out, wired exclusively from that exit port's edges. A junction always has a single upstream exit port; the elif is structurally unreachable. Reclassified candidate-dead -> defensive (#762): corpus instrumentation across 128 fixtures confirms the un-exercised arm is never taken. |
| 2717 | `for lid, off in desired.items():` | `->L2718`, `->L2719` | **defensive** -- Reaches the realignment body of _align_junction_to_entry_port only via a TB-reversed two-line bundle fanning into a junction (the #704 boundary fix is in, so this body is now provably reachable). The junction_entry_reversed_fold fixture now ships that topology; with the bundle order carried concentrically through the reversal corners the junction offsets already match their entry ports, so the realignment body short-circuits before its offset-writing arms and they stay un-exercised corpus-wide. Defensive. |
| 2719 | `if single_exit and feeding_exit is not None:` | `->L2658`, `->L2720` | **defensive** -- Reaches the realignment body of _align_junction_to_entry_port only via a TB-reversed two-line bundle fanning into a junction (the #704 boundary fix is in, so this body is now provably reachable). The junction_entry_reversed_fold fixture now ships that topology; with the bundle order carried concentrically through the reversal corners the junction offsets already match their entry ports, so the realignment body short-circuits before its offset-writing arms and they stay un-exercised corpus-wide. Defensive. Its False arm (->1147) is additionally defensive: a >=2-line junction reaching this body is always a single-exit-port-fed fan-out junction (merge junctions carry one line and are skipped by len(j_lines)<2). |
| 2721 | `if exit_lines == set(j_lines):` | `->L2658`, `->L2722` | **defensive** -- Reaches the realignment body of _align_junction_to_entry_port only via a TB-reversed two-line bundle fanning into a junction (the #704 boundary fix is in, so this body is now provably reachable). The junction_entry_reversed_fold fixture now ships that topology; with the bundle order carried concentrically through the reversal corners the junction offsets already match their entry ports, so the realignment body short-circuits before its offset-writing arms and they stay un-exercised corpus-wide. Defensive. Its lines-differ arm (->1147) is additionally defensive: a fan-out junction carries exactly its single exit port's fanned lines, so exit_lines == j_lines always. |
| 2723 | `if abs(exit_st.y - j_st.y) <= _SAME_Y_TOLERANCE:` | `->L2658`, `->L2724` | **defensive** -- Reaches the realignment body of _align_junction_to_entry_port only via a TB-reversed two-line bundle fanning into a junction (the #704 boundary fix is in, so this body is now provably reachable). The junction_entry_reversed_fold fixture now ships that topology; with the bundle order carried concentrically through the reversal corners the junction offsets already match their entry ports, so the realignment body short-circuits before its offset-writing arms and they stay un-exercised corpus-wide. Defensive. Its Y-differs arm (->1147) is additionally defensive: only a BOTTOM-exit junction is offset in Y from its exit, but such a junction sits below the consumer row and never meets the body's all-entries-within-tolerance precondition. |
| 2724 | `for lid, off in desired.items():` | `->L2658`, `->L2725` | **defensive** -- Reaches the realignment body of _align_junction_to_entry_port only via a TB-reversed two-line bundle fanning into a junction (the #704 boundary fix is in, so this body is now provably reachable). The junction_entry_reversed_fold fixture now ships that topology; with the bundle order carried concentrically through the reversal corners the junction offsets already match their entry ports, so the realignment body short-circuits before its offset-writing arms and they stay un-exercised corpus-wide. Defensive. |
| 2847 | `if pred is None:` | `->L2848` |  |
| 2943 | `if target_col is None:` | `->L2944` | **defensive** -- A registered LEFT entry port always has an owning section with a grid column after placement. The missing-target-column return protects a malformed or partially laid-out graph. |
| 2952 | `if source_col is None:` | `->L2953` | **defensive** -- _convergence_feeders receives resolved sources feeding a registered LEFT entry port. Each source is a sectioned port or a junction resolvable through one, so the missing-column arm protects an incomplete graph. |
| 3227 | `for _ in range(max_iterations):` | `->L-3199` | **defensive** -- _reconcile_horizontal_offsets iterates at most 10 times and breaks early when no change occurs. The arc L1338->(-1310) represents loop exhaustion without early-break (all 10 iterations consumed). On any valid parsed graph, the reconciliation converges in at most a handful of iterations because each iteration resolves at least one edge conflict or reaches a fixed point. The 10-iteration ceiling is a backstop against hypothetical oscillating graphs; normal topologies stabilize in 1-3 passes. |
| 3259 | `if src_n <= tgt_n:` | `->L3262` | **defensive** -- _reconcile_horizontal_offsets' bundle-shift fallback moves the edge endpoint with fewer lines. The else arm (src_n > tgt_n: move the target) was exercised in the corpus solely by the fan-port drag where a many-line entry/exit port is the edge source and a same-Y trunk-row station with fewer lines is the target (funcprofiler_upstream's humann3 at its entry port). The #1480 exit-port ordering fix (merge_trunk_lines in _compute_exit_port_offsets) rides the trunk-continuation merge line out on the trunk slot, so that port and station now agree and the edge no longer needs shifting. The remaining corpus shift (sr_qc into its input exit port) has the fewer-line station as the source, taking the src_n <= tgt_n true arm. The else arm stays defensive for a same-Y bundle conflict whose higher-line-count endpoint is the edge source. |
| 3315 | `if ctx.topology is None:` | `->L3316` |  |
| 3339 | `if key not in ctx.offsets:` | `->L3340` |  |
| 3388 | `len(levels) != len(continuing)` | `->L3391` |  |
| 3451 | `if line_id not in assignments:` | `->L3452` |  |
| 3456 | `and abs(owned - assignments[line_id]) > _OFFSET_EQ_TOLERANCE` | `->L3458` |  |
| 3478 | `for _iteration in range(len(ctx.graph.sections) + 1):` | `->L3503` |  |
| 3489 | `if line_id not in assignments:` | `->L3490` |  |
| 3496 | `if set(frames).difference(next_frames):` | `->L3497` | **defensive** -- This fixed-point transaction rollback handles an entry frame accepted in one iteration whose owner disappears in a later iteration. Valid settled graphs converge monotonically; only deliberately transient planner state reaches this arm. test_linear_entry_frame_settlement_restores_offsets_if_owner_disappears exercises the rollback through controlled planner output. |
| 3531 | `if any(` | `->L3535` |  |
| 3537 | `if local_offset is None or not (` | `->L3543` | **defensive** -- This post-acceptance marker-cache guard requires the sole local line to occupy exactly the adjacent lane above or below the continuing cohort. The accepted frame allocator always creates one such adjacent assignment. The missing or nonadjacent arm protects corrupted or incomplete offset maps. |
| 3562 | `if abs(actual - expected) > _OFFSET_EQ_TOLERANCE:` | `->L3563` |  |
| 3573 | `if gap is not None:` | `->L3574` |  |
| 3604 | `if any(` | `->L3610` | **defensive** -- This post-acceptance marker-cache guard rejects a continuing cohort whose inherited offsets are not one contiguous step sequence. _linear_entry_frame only accepts and materializes contiguous inherited levels, so authored topology cannot reach this reject arm. It protects corrupted or hand-built state. |
| 3628 | `if actual is None or abs(actual - assignment.offset) > _OFFSET_EQ_TOLERANCE:` | `->L3629` |  |
| 3800 | `if dag is not None:` | `->L3808` |  |
| 3803 | `for succ in dag.successors.get(stack.pop(), ()):` | `->L3804` |  |
| 3804 | `if succ not in affected:` | `->L3803`, `->L3805` |  |
| 3813 | `if not offs:` | `->L3814` |  |

## `orientation.py`

3/4 gates fully exercised.

Gates with an un-exercised arm:

| Line | Gate | Un-exercised arm(s) | Triage |
| ---: | --- | --- | --- |
| 56 | `if axis is DemandAxis.Y:` | `->L58` |  |

## `perp.py`

11/14 gates fully exercised.

Gates with an un-exercised arm:

| Line | Gate | Un-exercised arm(s) | Triage |
| ---: | --- | --- | --- |
| 122 | `if not feeders:` | `->L123` | **defensive** -- No-feeder guard in _perp_entry_crossing_x: the helper is called only from _route_perp_entry when a multi-line bundle shares the port with a per-line stagger, and a TOP/BOTTOM entry port in that case is always reached by a bundled inter-section feeder (dy!=0, so compute_bundle_info indexes it).  The empty-feeders return-None arm is a contract guard, never taken across the corpus. |
| 165 | `if entry_port is None or not entry_port.is_entry:` | `->L166` | **defensive** -- _aligned_horizontal_drop_entry only fires for a TOP/BOTTOM exit on a horizontal-flow section (the rest of _perp_riser_lateral's callers are caught earlier).  The return-None arm is taken when such an exit feeds a junction or several targets rather than one entry port; corpus instrumentation across every examples/ fixture finds no horizontal-flow perp exit feeding a non-single-entry, so the arm is never taken and the riser falls back to the perp reflection. |
| 172 | `if needs_perp_approach_fan(graph, entry_port.id):` | `->L173` | **defensive** -- _aligned_horizontal_drop_entry exempts a distinct-line approach fan, whose intra drop fans onto per-line channels (_perp_approach_fan_x) rather than the entry's single per-line offset.  Reaching it needs a horizontal-flow perp exit dropping straight into a single approach-fan entry; corpus instrumentation finds none, so the arm is never taken and the riser keeps the perp reflection. |

## `planning.py`

0/1 gates fully exercised.

Gates with an un-exercised arm:

| Line | Gate | Un-exercised arm(s) | Triage |
| ---: | --- | --- | --- |
| 78 | `if scaffold is None:` | `->L79` |  |

## `postprocess.py`

96/112 gates fully exercised.

Gates with an un-exercised arm:

| Line | Gate | Un-exercised arm(s) | Triage |
| ---: | --- | --- | --- |
| 211 | `if abs(dx_diag) <= COORD_TOLERANCE_FINE:` | `->L212` | **defensive** -- Defensive guard: a near-vertical baked diagonal (dx_diag ~ 0) already separates near-fully along the baked X axis, so it needs no Y spread and the divide-by-dx_diag is skipped. No corpus fixture bakes a near-vertical diagonal, so the guard arm is never taken. |
| 443 | `if not is_fork_join and (` | `->L452` | **defensive** -- Enters the multi_diag block, which needs a non-fork-join station (<=1 distinct predecessor and successor) carrying both a 4-point diagonal and a 2-point flat incoming route. Raw route shape is a pure function of endpoint coordinates, so all lines on one edge share one shape: n_unique_in and n_unique_flat_in can never both be >=1 for a single-predecessor station, and n_unique_in/out are <=1 when not fork-join. multi_diag is unsatisfiable (0 of 6173 corpus calls). Reclassified candidate-dead -> defensive (#762): corpus instrumentation across 128 fixtures confirms the un-exercised arm is never taken. |
| 493 | `if multi_diag:` | `->L494` | **defensive** -- multi_diag branch of _flat_connects_to_internal_chain; multi_diag is structurally unsatisfiable (see the _classify_centering_routes gate at the same line text), so the function is only ever called with multi_diag False. Reclassified candidate-dead -> defensive (#762): corpus instrumentation across 128 fixtures confirms the un-exercised arm is never taken. |
| 494 | `if any(_is_chain_predecessor(graph, ctx, r.edge.source) for r in flat_in):` | `->L495`, `->L496` | **defensive** -- Inside the multi_diag block of _flat_connects_to_internal_chain, which is never entered because multi_diag is structurally unsatisfiable. Both arms unreachable. Reclassified candidate-dead -> defensive (#762): corpus instrumentation across 128 fixtures confirms the un-exercised arm is never taken. |
| 496 | `if any(_is_internal_station(graph, r.edge.target) for r in flat_out):` | `->L497`, `->L498` | **defensive** -- Inside the multi_diag block of _flat_connects_to_internal_chain, never entered because multi_diag is structurally unsatisfiable. Both arms unreachable. Reclassified candidate-dead -> defensive (#762): corpus instrumentation across 128 fixtures confirms the un-exercised arm is never taken. |
| 537 | `if abs(v) < COORD_TOLERANCE_FINE:` | `->L538` |  |
| 646 | `if multi_diag:` | `->L647` | **defensive** -- multi_diag branch selecting the flat-segment X extent in _centering_candidate; multi_diag is structurally unsatisfiable (see the _classify_centering_routes gate). Reclassified candidate-dead -> defensive (#762): corpus instrumentation across 128 fixtures confirms the un-exercised arm is never taken. |
| 659 | `if not multi_diag:` | `->L666` | **defensive** -- The skip-this-block arm fires only when multi_diag is True, which is structurally unsatisfiable (see the _classify_centering_routes gate). Reclassified candidate-dead -> defensive (#762): corpus instrumentation across 128 fixtures confirms the un-exercised arm is never taken. |
| 669 | `if abs(in_flat) < 1 or abs(out_flat) < 1:` | `->L670` |  |
| 777 | `if abs(other.y - station.y) > 1:` | `->L767` | **defensive** -- The loop-back arm (skip appending a column companion) fires only when another non-port station in the same section shares the moving station's x (within 1px) and its y (within 1px) - two near-coincident distinct stations, forbidden by the no-overlap invariant. |
| 820 | `if ox is None:` | `->L821` | **defensive** -- Null guard in _align_uncentered_siblings. The loop iterates visible non-port stations and original_x is built from every non-port station, so ox is always present; the guard never fires. |
| 847 | `if max(moved_xs) - min(moved_xs) <= 1.0:` | `->L848` | **defensive** -- Early-exit for the agree case: all movers land within 1px of each other. Unreachable for natural pipelines because the engine always seats one fan branch on the hub's trunk row, vetoing companion consensus for the whole column, so moved is empty and the outer guard fires first. |
| 853 | `if majority_count <= len(moved) / 2:` | `->L854` | **defensive** -- The no-clear-majority arm. Movers in a column either converge to one consensus x or sit off it only via hub/exit-port collinearity at a fixed >=20px quantum; the consensus cluster is always the strict majority, so majority_count is never <= half (0 of corpus, no probe topology reproduces a split). Reclassified candidate-dead -> defensive (#762): corpus instrumentation across 128 fixtures confirms the un-exercised arm is never taken. |
| 860 | `if not outliers:` | `->L861` | **defensive** -- The no-outliers arm in the movers-disagree branch. Disagreement only arises from hub/exit-port collinearity at a >=20px quantum, never sub-pixel, so whenever spread exceeds 1px at least one mover rounds >1px from the majority x. outliers is never empty here. Reclassified candidate-dead -> defensive (#762): corpus instrumentation across 128 fixtures confirms the un-exercised arm is never taken. |
| 869 | `if abs(rp.points[0][0] - old_x) < STATION_MOVE_TOLERANCE:` | `->L868` | **defensive** -- Per-route anchor guard: only an outgoing route whose start sits at the dragged station's old x is re-pointed to the new x. The skip arm protects against re-pointing a route not anchored at the station (e.g. an L-shape or bypass leg not snapped by _apply_station_moves); dragged stations in the corpus only carry position-anchored routes, so it never fires. |
| 872 | `if abs(rp.points[-1][0] - old_x) < STATION_MOVE_TOLERANCE:` | `->L871` | **defensive** -- Per-route anchor guard: only an incoming route whose end sits at the dragged station's old x is re-pointed to the new x. The skip arm protects against re-pointing a route not anchored at the station; dragged stations in the corpus only carry position-anchored routes, so it never fires. |

## `rail.py`

17/23 gates fully exercised.

Gates with an un-exercised arm:

| Line | Gate | Un-exercised arm(s) | Triage |
| ---: | --- | --- | --- |
| 56 | `if st is None:` | `->L57` | **defensive** -- _line_rail_y is only ever called with a real station id (on_rail.id, edge.source/target, or a sibling edge endpoint), all of which exist in graph.stations - the route loop pre-filters None endpoints at the src/tgt guard. The None arm is a contract guard no render-path call reaches. |
| 64 | `if line_id in section_rails:` | `->L67` | **defensive** -- The whole-graph rail connector routes port-to-port edges through _line_rail_y, exercising the in-rails arm. The fall-through (a port carrying a line absent from its own section's rail map) cannot occur: a port carries only lines its section's stations carry, all present in per_line_y. Defensive fallback. |
| 75 | `if line_id in served and len(st.rail_used_ys) == len(served):` | `->L77` | **defensive** -- Reached only for a non-port spanning station, whose rail_used_ys is station_lines_ordered filtered to the section's per_line_y - itself built from the same _section_lines_in_order. So a served line is always in per_line_y (lengths match) and _line_rail_y is only called with a line the touching edge carries (always served). The fall-through return st.y is a defensive fallback no render-path call reaches. |
| 131 | `if sib_rails:` | `->L136` | **defensive** -- _off_track_drop_order is only called for an off-track elbow edge that exists in graph.edges, so the feeder<->consumer set it scans always contains at least that edge; the empty-sib_rails arm (skip the mirror-corner reversal) is a contract guard no render-path call reaches. |
| 259 | `if exit_port.side is not PortSide.RIGHT or entry_port.side is not PortSide.LEFT:` | `->L260` | **defensive** -- Whole-graph rail mode stacks sections vertically and resolve.py always sides an inter-section edge as a RIGHT exit feeding a LEFT entry, so the early-return arm (an unmodelled side pairing) is a structural fallback no rail topology reaches. |
| 358 | `if conn is not None:` | `->L354` | **defensive** -- Skips the straight-rail fallback when _route_inter_section_connector handled a port-to-port bundle. The connector returns a route for every whole-graph rail inter-section edge (always a RIGHT exit feeding a LEFT entry), so the None arm (fall through to straight-rail routing) is a structural fallback no rail topology reaches. |

## `reserved_bands.py`

7/10 gates fully exercised.

Gates with an un-exercised arm:

| Line | Gate | Un-exercised arm(s) | Triage |
| ---: | --- | --- | --- |
| 54 | `if self.hi < self.lo - COORD_TOLERANCE:` | `->L55` | **defensive** -- The invariant the router relies on: a band handed to _center_inter_row_channel or centre_inter_column_channel always holds a channel, so the narrow-gap fallback cannot be reached for a claimed corridor. build_reserved_corridors filters an empty intersection out before constructing a band, so the raise guards direct construction only. |
| 152 | `if boundary is None:` | `->L153` | **defensive** -- Optional-boundary convenience for callers carrying an unresolved grid index (_left_exit_wrap_over_top_y takes tgt_row: int \| None). Such a caller returns before it needs a band -- with no target row there is no gap above it to measure, so the wrap is rejected first -- leaving this arm a type-level accommodation no topology reaches. |
| 241 | `if realised is None:` | `->L242` | **defensive** -- realise_reservation returns None only for a canvas-side corridor measured without canvas bounds; this loop has already narrowed to row-gap or column-gap claims, which always measure. The arm satisfies the optional return type rather than a reachable state. |

## `reversal.py`

42/52 gates fully exercised.

Gates with an un-exercised arm:

| Line | Gate | Un-exercised arm(s) | Triage |
| ---: | --- | --- | --- |
| 200 | `if not src.is_port:` | `->L201` |  |
| 220 | `if receiver not in reversed_secs:` | `->L219` | **defensive** -- BFS dedup guard in _detect_tb_bottom_top_entries: the False arm fires when a TB section is queued as a vertical receiver via two different positive_fan paths. The detection only registers a receiver when a BOTTOM exit PORT directly feeds a TOP entry port (edge.source is the exit port). A TB section reached by two upstream sections is fed through a fork/merge junction, so the edges into its TOP entry originate at the junction, not a port -- the detection skips them and the receiver is never queued twice. A diamond renders (examples/topologies/tb_bottom_exit_fork_diamond.mmd) but does not drive this arm. The guard is correct and necessary to prevent double-queueing and ensure BFS termination should a direct double-feed ever arise. |
| 257 | `if jid in seen:` | `->L258` | **defensive** -- Cycle guard in _entry_ports_through_junctions' junction walk. The True arm fires only if a junction is reached twice while walking out from one exit-port-fed junction (a junction reachable by two paths). Resolved peel-off fans are trees, so no junction is revisited and the arm is unreached; the guard keeps the walk terminating should chained junctions ever form a cycle. |
| 265 | `if port and port.is_entry:` | `->L260` | **defensive** -- _entry_ports_through_junctions collects the entry ports a peel-off junction fan lands on. Each junction out-edge targets either another junction (consumed by the prior `edge.target in junction_ids` continue) or an entry port; no junction fans directly to a station or exit port across the corpus. The non-entry arm is therefore a structural-contract guard never reached. |
| 314 | `if entry and entry.section_id and entry.section_id != src.section_id:` | `->L310` | **defensive** -- _build_section_adjacency records a section successor reached through a peel-off junction. entry_id comes from _entry_ports_through_junctions, which returns only registered entry-port stations, so graph.stations.get(entry_id) is always present and carries a section_id. The only un-exercised arm is entry.section_id == src.section_id -- a junction fanning back into its own source section -- which a resolved peel-off fan never produces. Defensive structural-contract guard. |
| 340 | `if not section:` | `->L341` | **defensive** -- _propagate_reversal_along_rows iterates sec_id values drawn from reversed_secs, which only ever holds real section ids added from graph.sections. graph.sections.get(sec_id) is never None; the continue is a defensive null guard. |
| 351 | `if not succ:` | `->L352` | **defensive** -- succ_id comes from sec_successors, built only from real tgt.section_id values, so graph.sections.get(succ_id) is never None and the continue (->187) is a defensive null guard. The fall-through static arm (->188) is phantom: control falls into the following multi-line `if (`, whose first operand line carries the real arc L186->189. |
| 355 | `or (sec_id, succ_id) in horizontal_succ_pairs` | `->L347` |  |
| 411 | `if not s2.is_port:` | `->L412` |  |
| 418 | `elif src.is_port:` | `->L405` | **defensive** -- Edge feeding a LEFT/RIGHT entry port whose source is not a junction; src is then always an exit port (is_port=True), because resolved chains feed entry ports only from exit ports or junctions. The elif is always true, so the implicit fall-through to the loop (->223) is a structural-contract guard no valid topology reaches. |

## `seam.py`

6/6 gates fully exercised.

All gates have every arm exercised by the corpus.

## `system_emission.py`

23/48 gates fully exercised.

Gates with an un-exercised arm:

| Line | Gate | Un-exercised arm(s) | Triage |
| ---: | --- | --- | --- |
| 58 | `if compatible != bool(self.compatibility_reasons):` | `->L59` |  |
| 67 | `if member_reservation_ids != self.reservation_ids:` | `->L68` |  |
| 204 | `if geometry_failure is not None:` | `->L205` |  |
| 290 | `if prior != ownership.covered_by_member_id:` | `->L291` |  |
| 406 | `if owners != 1:` | `->L407` |  |
| 415 | `if len(by_edge) != sum(len(system.members) for system in frozen):` | `->L416` |  |
| 443 | `for covered_edge_key, carrier_edge_key in covered_routes:` | `->L444` |  |
| 446 | `if covered_member is None or carrier_member is None:` | `->L447`, `->L448` |  |
| 451 | `if prior != carrier_member.member_id:` | `->L443`, `->L452` |  |
| 460 | `route.route_system_id is not None` | `->L463` |  |
| 461 | `or route.emission_member_id is not None` | `->L463` |  |
| 467 | `if member is None:` | `->L468` |  |
| 478 | `route.route_system_id != str(system.system_id)` | `->L484` |  |
| 479 | `or route.emission_member_id != str(member.member_id)` | `->L484` |  |
| 480 | `or route.route_system_disposition != system.disposition.value` | `->L484` |  |
| 481 | `or route.route_plan_ids != system.plan_ids` | `->L484` |  |
| 482 | `or route.route_reservation_ids != member.reservation_ids` | `->L484` |  |
| 496 | `if count:` | `->L497` |  |
| 502 | `if covering_binding is None:` | `->L503` |  |
| 508 | `if covering_system.system_id != system.system_id:` | `->L509` |  |
| 514 | `if emitted.get(covering_member_id, 0) != 1:` | `->L515` |  |
| 520 | `if count != 1:` | `->L521` |  |
| 538 | `if system is None:` | `->L539` |  |
| 547 | `if member is None or member.system_id != system.id:` | `->L548` |  |
| 555 | `if route.route_reservation_ids != expected:` | `->L556` |  |

## `tb_handlers.py`

16/20 gates fully exercised.

Gates with an un-exercised arm:

| Line | Gate | Un-exercised arm(s) | Triage |
| ---: | --- | --- | --- |
| 156 | `if diag_end < diag_start:` | `->L157` | **defensive** -- Degenerate-collapse clamp for an ascending diagonal: fires only when the run is shorter than 2*MIN_STRAIGHT_EDGE (20px). Every caller feeds grid-placed station coordinates whose run-axis separation is at least one grid pitch (Y_SPACING=40 / X_SPACING=60); _route_entry_runway additionally pre-guards with `room < src_min + diagonal_run`. The 20px collapse threshold is never reached. |
| 162 | `if diag_end > diag_start:` | `->L163` | **defensive** -- Descending-run mirror of the ascending collapse clamp (#1 of `if diag_end < diag_start:`): fires only when \|run\| < 2*MIN_STRAIGHT_EDGE (20px), but connected diagonal endpoints are always >= one grid pitch (40/60px) apart on the run axis. Unreachable degenerate-geometry guard. |
| 372 | `if tgt_sec is not None and tgt_sec.direction not in ("TB", "BT"):` | `->L376` | **defensive** -- Perp-entry drop crossing-X override: the edge's target is always a sectioned station, so tgt_sec resolves and the `tgt_sec is None` short-circuit to the plain drop is never taken; the `direction in (TB, BT)` arm (a drop continuing into a vertical-flow trunk) and the horizontal-target arm are both exercised. |
| 374 | `if crossing_x is not None:` | `->L376` | **defensive** -- Pair of context.py::if not indices: - the helper returns None only when no bundled feeder reaches the port, but a horizontal-flow section taking a perpendicular entry is always fed by a bundled inter-section edge, so crossing_x resolves.  The crossing_x-is-None fall-through is a defensive guard, never taken across the corpus. |
